# 06 — Verification checklist (Azure → GCP `boot-464019`)

Date: **2026-09-16**, revision 3 (companion to 05 rev 3; reflects the owner decisions of 2026-09-16 — no feature branch, kradle bootstrap-only, gcloud already authenticated — review 1's R-1..R-8 and review 2's N-1..N-7: `pg-restore` identity rows, the real chart/api bootstrap state (0 `Organization`/`User`/`Repository` CRs; X-D.4a, X-E.16, X-E.18 → X-F.11, new X-F.5a), one `KRADLE_TEST_AUTH_SECRET` value written in Phase C (X-C.7, new X-C.10/X-C.11, X-G.3a moved), corrected `psql`/grep/pipe commands and expected values — every command below was checked against `staging` @ `57b13a4c5`). Companion to [`05-migration-plan.md`](./05-migration-plan.md); every phase there exits only when its rows here are PASS. Rows are objectively verifiable: an exit code, an HTTP status, a count, a `kubectl rollout status`, a `gcloud … describe`; the few manual/browser/console rows are marked **manual** with the evidence they require. Evidence goes to `docs/migration/azure-to-gcp/raw/verify/<ID>.txt` (command + output, secrets redacted — never a secret **value**; where a command would print one, the row says how to avoid it). Placeholders: `<IP>` = the platform terraform output `ingress_ip` (recorded in `reviews/05-phase-b-log.md`; it is **not** a GitHub variable), `<num>` = project number, `<docs-commit>` = the sha of the docs commit on `staging` (05 §0 chain), `$DATE` as in 05 §5. Kube contexts: AKS `aks-hub-development-westus3-v2` (read-only), GKE `gke_boot-464019_us-central1-a_a5c-boot-gke` — created once by `gcloud container clusters get-credentials a5c-boot-gke --zone us-central1-a --project boot-464019` (needs `gke-gcloud-auth-plugin`, X-0.1); that is how the context name appears in `kubectl config get-contexts`.

Coverage index (everything the run's instructions require to appear):

- **Hostnames** (all 14 current + target ones): `kradle.a5c.ai`, `gitea.kradle.a5c.ai`, `kradle-staging.a5c.ai`, `gitea.kradle-staging.a5c.ai`, `atlas.a5c.ai`, `atlas-staging.a5c.ai`, `gke-canary.a5c.ai`, `benchmarks.a5c.ai`, `geekonomy.a5c.ai`, `studio.a5c.ai`, `breakpoints-pro.a5c.ai` (D-9 only), `meet.kradle.local` (internal), decommissioned: `chat`, `docs`, `aeq`, `bmux`, `app`, `app-jaeger`, `app-staging`, `app-jaeger-staging`, `install-staging`, `hub`, `staging.web` (all `.a5c.ai`), untouched: `a5c.ai`, `www.a5c.ai` → rows X-F.1–X-F.4, X-G.*.
- **Stateful items** (01 §2.1/§4.11): atlas ×2 PG, content-studio PG, aeq Azure PG, hub in-cluster PG, hub Azure PG, litellm PG ×2 + shares, geekonomy PG + Redis, a5c-app PG ×2, tfstate, trial-telemetry, hub blobs + 1 TiB share, mantis evidence/album, `a5cstatic`, AzureML shares, kradle CRs ×2 (confirmed **no export** — bootstrap-only), gitea ×2 (no export), `hub-redis-pvc`, prosody/aeq Pending PVCs → rows X-E.*.
- **Workflow jobs**: babysitter `publish.yml` `deploy_atlas_webui`, `deploy_kradle`, `e2e_smoke_kradle`, `g0_rt_jitsi_e2e`, `deploy_staging_cloud`; `g0-rt-jitsi-e2e.yml`; `live-stack.yml`, `live-stack-published.yml`, the 12 agent/qa/triage workflows; infra `apply-gcp.yml` (`platform`, `addons`); geekonomy `deploy-gke.yml`; benchmarks `publish-reports.yml`; company `content-studio-deploy.yml`; osb `build-images.yml`, `cd.yml`; askExpertQuestion `deploy.yml`, `terraform.yml`; hub/claude-web/a5c-website Azure workflows (disabled) → rows X-A.*, X-B.*, X-D.*, X-H.4.
- **GitHub variables/secrets**: created (C.1/C.2) and retired (C.4) → rows X-C.*, X-H.4.
- **CRD controllers** (kradle `controllers` deployment modules + platform controllers) → rows X-F.10–X-F.14.

---

## Phase 0 — Preconditions

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-0.1 | SDK + GKE auth plugin (**remaining**; the update must be run from an **elevated shell** — 03 §3.1: the component manager is blocked by directory permissions on `C:\Program Files (x86)\Google\Cloud SDK`) | `gcloud version && gke-gcloud-auth-plugin --version && gcloud components list --filter=id:beta --format='value(state.name)'` | version lines; `Installed` | gcloud ≥ 500.0.0; plugin prints a version; `beta` installed (needed by X-0.5); exit 0 | `raw/verify/X-0.1.txt` |
| X-0.2 | Project reachable as the owner account (**done 2026-09-16** — cheap re-check) | `gcloud auth list --filter=status:ACTIVE --format='value(account)'; gcloud config get-value project; gcloud projects describe boot-464019 --format='value(projectId,lifecycleState)'` | `tal@a5c.ai`; `boot-464019`; `boot-464019 ACTIVE` | exact strings; exit 0 | same pattern for every row below |
| X-0.3 | ADC works (**the remaining real precondition** for terraform) | `gcloud auth application-default print-access-token >/dev/null && echo ADC_OK` | `ADC_OK` | exit 0 | — |
| X-0.4 | Operator roles | `gcloud projects get-iam-policy boot-464019 --flatten='bindings[].members' --filter="bindings.members:$(gcloud config get-value account)" --format='value(bindings.role)'` | list of roles | contains `roles/owner` or every role in 03 §5.1 | — |
| X-0.5 | Billing linked and open | `gcloud billing projects describe boot-464019 --format='value(billingEnabled)'` | `True` | exact | — |
| X-0.6 | GitHub org admin | `gh api orgs/a5c-ai/memberships/$(gh api user -q .login) -q .role` | `admin` | exact | — |
| X-0.7 | Azure read access intact | `az account show --query id -o tsv` | subscription id | `794e33cd-48ba-4654-8cd9-b6cf2715e097` | — |
| X-0.8 | GoDaddy TTL lowered, zone snapshot | `dig +noall +answer '*.a5c.ai' A @ns41.domaincontrol.com` (wildcard query) and `dig +short random-$RANDOM.a5c.ai @ns41.domaincontrol.com` | TTL column ≤ 600; `135.234.117.214` | both | `raw/dns/godaddy-zone-before.txt` |
| X-0.9 | Owner decisions recorded (18 — D-16 and D-21 are superseded and not counted) | `grep -cE '^\| D-(4\|7\|8\|9\|10\|11\|12\|13\|14\|15\|18\|19\|22\|23\|24\|25\|26\|27) ' docs/migration/azure-to-gcp/decisions.md; grep -cE '^\| D-(16\|21) .*superseded' docs/migration/azure-to-gcp/decisions.md` | `18`; `2` | exact | file |
| X-0.10 | GCP reuse audit done (**done 2026-09-16**, 03 §3.1 — greenfield) | `test -s docs/migration/azure-to-gcp/raw/gcp/_summary.tsv && grep -c 'greenfield' docs/migration/azure-to-gcp/03-gcp-readiness.md` | ≥ `1` | file present and the word appears in §3.1 | `_summary.tsv` |

## Phase A — Source changes (`a5c-ai/babysitter`, committed on `staging` locally; X-A.1–X-A.17, X-A.19–X-A.25 run **before the push**, X-A.18 after it)

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-A.1 | Tests were red before implementation (verified **locally**, never by a push) | `git log --format='%h %s' <docs-commit>..HEAD` on `staging` — the **first** line (oldest, `--reverse`) is the tests commit; then `git stash; git checkout $(git log --format=%h --reverse <docs-commit>..HEAD \| head -1); npm run test:kradle; ( cd packages/atlas/webui && npm run test:smoke ); git checkout staging; git stash pop` | failures in T1, T3 (and T2/T4/T5 missing targets) at the tests commit; all green at HEAD | non-zero exit at that commit, zero at HEAD | verifier report |
| X-A.2 | kradle core tests (T3–T7) | `npm run test:kradle` | `# fail 0` | exit 0 | output |
| X-A.3 | atlas webui tests (T1, T2) | `( cd packages/atlas/webui && npm run test:smoke )` | all passed | exit 0 | output |
| X-A.4 | installer tests (T9) | `( cd packages/kradle/installer && npm test )` | passed | exit 0 | output |
| X-A.5 | live-stack contract tests (T10, after A.9) | `npx vitest run packages/adapters/cli/tests/live-stack/` | passed | exit 0 | output |
| X-A.6 | Chart lints and renders without pull secrets, with staging prefix | `helm lint packages/kradle/charts && helm template kradle packages/kradle/charts --set atlasBaseUrl=http://atlas-webui.atlas-staging.svc.cluster.local --set global.orgNamespacePrefix=kradle-org-staging- --set externalSecrets.enabled=true --set externalSecrets.secretPrefix=kradle-staging > /tmp/k.yaml && grep -c imagePullSecrets /tmp/k.yaml; grep -c 'name: kradle-org-staging-a5c-ai' /tmp/k.yaml; grep -c 'kind: ExternalSecret' /tmp/k.yaml` | `0` / `≥1` / `≥4` | as stated | output |
| X-A.7 | `atlasBaseUrl` is required | `helm template kradle packages/kradle/charts >/dev/null 2>&1; echo $?` | non-zero, message contains `atlasBaseUrl is required` | exit ≠ 0 | output |
| X-A.8 | Every chart workload has requests | `helm template … (as X-A.6) \| yq ea 'select(.kind=="Deployment" or .kind=="StatefulSet") \| .spec.template.spec.containers[] \| select(.resources.requests == null) \| .name' \| wc -l` | `0` | exact | output |
| X-A.9 | Atlas kustomize renders cleanly | `kubectl kustomize packages/atlas/webui/deploy/overlays/atlas-staging \| grep -E 'storageClassName\|imagePullSecrets\|file\.csi\|external-dns\|azure'` | only `storageClassName: standard-rwo` lines | no other match | output |
| X-A.10 | Workflow hygiene (publish + jitsi e2e + chart + core vendored) | `git grep -nE 'azurecr\|AZURE_ACR\|KUBE_CONFIG\|acr-pull\|az acr\|azure/login\|file\.csi\.azure\|atlas-postgres-static\|ATLAS_POSTGRES_STORAGE\|AZURE_API_KEY\|AGENT_MUX_API_BASE\|external-dns' -- .github/workflows/publish.yml .github/workflows/g0-rt-jitsi-e2e.yml packages/atlas/webui/deploy packages/kradle/charts packages/kradle/core/.github` | no output | exit 1 from grep (no match) | output |
| X-A.11 | Job names | `grep -nE 'name: Deploy (Atlas WebUI\|Kradle) To GKE' .github/workflows/publish.yml \| wc -l` | `2` | exact | output |
| X-A.12 | Both deploy jobs use WIF + GKE creds + AR | `awk '/^  deploy_atlas_webui:/,/^  deploy_kradle:/' .github/workflows/publish.yml \| grep -cE 'google-github-actions/(auth\|setup-gcloud\|get-gke-credentials)@v2'` and the same for `deploy_kradle` → `e2e_smoke_kradle` | `3` and `3` | exact | output |
| X-A.13 | No `:latest` pushes | `grep -c ':latest' .github/workflows/publish.yml` | `0` | exact | output |
| X-A.14 | Re-stamp, kubevela, from-literal blocks gone | `grep -cE 'helm uninstall kubevela\|meta.helm.sh/release-name.*kradle-org\|--from-literal' .github/workflows/publish.yml` | `0` | exact | output |
| X-A.15 | Provider lanes retired (after A.9) | `git grep -nE 'AZURE_API_KEY\|AGENT_MUX_API_BASE\|foundry-openai' -- .github/workflows packages/adapters/cli/tests/live-stack \| wc -l` | `0` | exact | output |
| X-A.16 | actionlint | `actionlint .github/workflows/publish.yml .github/workflows/g0-rt-jitsi-e2e.yml .github/workflows/live-stack.yml .github/workflows/live-stack-published.yml` | no findings | exit 0 | output |
| X-A.17 | Build + metadata | `npm run build:sdk && npm run verify:metadata` | ok | exit 0 | output |
| X-A.18 | CI green on `staging` (**after the push** — `ci.yml` is `pull_request`-only and never runs on a direct push; this gates the `main` promotion, D.3) | `gh workflow run ci.yml --ref staging && gh run watch` | success | conclusion `success` | run URL |
| X-A.19 | Diff scope + no secrets + lockfile | `git diff <docs-commit>...HEAD --stat`; `git diff <docs-commit>...HEAD \| grep -ciE 'BEGIN (RSA\|PRIVATE)\|api[_-]?key\s*[:=]\s*[A-Za-z0-9]{20,}'`; `git diff <docs-commit>...HEAD -- package-lock.json \| grep -c win32` | files only from 05 A.2–A.10; `0`; `0` | all | verifier report |
| X-A.20 | Docs updated | `grep -c 'deploys to AKS' docs/development/06-kradle-cloud-platform.md; grep -c 'emptyDir on AKS\|emptyDir-on-AKS' docs/research/g0-rt-live-validation-runbook.md` | `0` / `0` | exact | output |
| X-A.21 | Vendored workflow rewritten | `grep -cE 'AZURE_ACR_NAME\|KUBE_CONFIG\|acr-pull' packages/kradle/core/.github/workflows/publish.yml` | `0` | exact | output |
| X-A.22 | `deploy_staging_cloud` untouched (dormant) | `git diff <docs-commit>...HEAD -- .github/workflows/publish.yml \| grep -c 'deploy_staging_cloud'` | `0` | exact | output |
| X-A.23 | No CR export/import lane was written (superseded 2026-09-16) | `ls packages/kradle/core/scripts/cr-export.mjs packages/kradle/core/scripts/cr-import.mjs packages/kradle/core/src/cr-migration.js packages/kradle/core/tests/cr-migration.test.js 2>&1 \| grep -c 'No such file'; grep -c 'kradle:cr-' packages/kradle/core/package.json` | `4`; `0` | exact | output |
| X-A.24 | *(deleted 2026-09-16 — fail-closed kind rule belonged to the CR lane)* | — | — | — | — |
| X-A.25 | Independent verifier verdict (gates the push) | read `reviews/05-phase-a-verification.md` | `Verdict: PASS` | exact | file |
| X-A.26 | Workstation hooks cannot push on commit (git hooks **and** Claude Code hooks — the historical auto-commit+push hook was a Claude Code hook, memory `project_autocommit_push_hook`); one push only | `git config --get core.hooksPath; ls .githooks; grep -lE '^[^#]*git push' .githooks/* \| wc -l` (non-comment lines only — `.githooks/pre-push:5` mentions `git push --no-verify` in a comment, which is why a plain `grep -l 'git push'` yields `1` today); `jq -r '..\|.command? // empty' .claude/settings.json .claude/settings.local.json ~/.claude/settings.json 2>/dev/null \| grep -cE 'git (commit\|push)'`; `grep -cE 'git (commit\|push)' ~/.claude/agent-flow/hook.js` (the only PostToolUse hook on this workstation; if the file does not exist, record `absent`); after the push: `git log --format=%h origin/staging --not <docs-commit> \| wc -l` equals `git log --format=%h <docs-commit>..HEAD \| wc -l` | `.githooks`; `pre-commit pre-push`; `0`; `0`; `0` (or `absent`); equal counts (all Phase A commits arrived in one push) | all (all five hold at `57b13a4c5`: `.claude/settings.json` is `{}`, no `hooks` entry of `settings.local.json` runs `git commit`/`git push` (its `hooks` is `{}`; it also holds `permissions`, `enabledMcpjsonServers`, `enabledPlugins`), the global hook has no git call) | output |

## Phase B — GCP foundation

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-B.0 | Assumptions re-verified | `gcloud services list --enabled --project boot-464019 --format='value(config.name)' \| grep -cE '^(container\|compute\|artifactregistry\|iam\|iamcredentials\|sts\|secretmanager\|sqladmin\|servicenetworking\|firebase\|firebasehosting\|gkebackup\|aiplatform)\.googleapis\.com$'`; `gcloud compute regions describe us-central1 --format='value(quotas)' \| tr ';' '\n' \| grep -E 'CPUS\|IN_USE_ADDRESSES'`; `gcloud resource-manager org-policies list --project boot-464019` | before the platform apply: `4` (03 §3.1: only `compute`, `iam`, `iamcredentials`, `aiplatform` of the 13 are enabled); after: `13`; CPUS limit ≥ 32, addresses ≥ 2; no blocking policy | as stated — record both counts | 03 §3.1 |
| X-B.1 | State bucket | `gcloud storage buckets describe gs://boot-464019-tfstate --format='value(versioning.enabled,uniformBucketLevelAccess.enabled)'` | `True True` | exact | output |
| X-B.2 | PR plan behaviour | `apply-gcp.yml` run on the PR: `platform` job shows a plan; `addons` job log contains `platform state not found; addons plan skipped` | notice present; no failure | both jobs `success` | run URL |
| X-B.3 | Platform applied, plan clean, no destroys ever | `terraform -chdir=terraform/envs/gcp-boot/platform plan -detailed-exitcode` (via CI dispatch or locally with ADC) | `No changes.` | exit 0; and the apply log of the first run contains `0 to destroy` | run URL |
| X-B.4 | Addons applied, plan clean | same for `addons` | `No changes.` | exit 0 | run URL |
| X-B.5 | Cluster shape | `gcloud container clusters describe a5c-boot-gke --zone us-central1-a --format='value(location,releaseChannel.channel,workloadIdentityConfig.workloadPool,privateClusterConfig.enablePrivateNodes,masterAuthorizedNetworksConfig.enabled,addonsConfig.gkeBackupAgentConfig.enabled,loggingConfig.componentConfig.enableComponents)'` | `us-central1-a REGULAR boot-464019.svc.id.goog True <empty/False> True SYSTEM_COMPONENTS;WORKLOADS` | all fields | output |
| X-B.6 | Node pools + SA + Spot taint | `gcloud container node-pools list --cluster a5c-boot-gke --zone us-central1-a --format='table(name,config.machineType,config.spot,config.serviceAccount,autoscaling.minNodeCount,autoscaling.maxNodeCount,config.taints)'` | `system e2-standard-4 False gke-node@… 2 4 -`; `agents e2-standard-4 True gke-node@… 0 4 a5c.ai/agents=true:NoSchedule` | both rows | output |
| X-B.7 | Artifact Registry repos + node pull role | `gcloud artifacts repositories list --location us-central1 --format='value(name,format)' \| grep -cE '(a5c-images\|geekonomy).*DOCKER'`; `gcloud artifacts repositories get-iam-policy a5c-images --location us-central1 --flatten='bindings[].members' --filter='bindings.role:roles/artifactregistry.reader' --format='value(bindings.members)'` | `2`; contains `serviceAccount:gke-node@boot-464019.iam.gserviceaccount.com` | both | output |
| X-B.8 | Static IP reserved and attached | `gcloud compute addresses describe ingress-a5c --region us-central1 --format='value(address,status)'`; `kubectl -n ingress-nginx get svc ingress-nginx-controller -o jsonpath='{.status.loadBalancer.ingress[0].ip}'` | `<IP> IN_USE`; `<IP>` | equal | output |
| X-B.9 | WIF pool/providers + bindings (N-2) | `gcloud iam workload-identity-pools providers list --workload-identity-pool github --location global --format='value(name,attributeCondition)'`; `gcloud iam service-accounts get-iam-policy github-terraform@boot-464019.iam.gserviceaccount.com --format=json \| jq -r '.bindings[]\|select(.role=="roles/iam.workloadIdentityUser")\|.members[]'` | providers `a5c-ai` (`assertion.repository_owner == "a5c-ai"`) and `external` (`assertion.repository in [...]`); terraform SA member is exactly `principal://iam.googleapis.com/projects/<num>/locations/global/workloadIdentityPools/github/subject/repo:a5c-ai/infra:ref:refs/heads/main`; same command for `geekonomy-terraform@` → exactly `…/subject/repo:Benihakak/geekonomy:ref:refs/heads/main`; for `geekonomy-deployer@` → exactly one member, `principalSet://…/attribute.repository/Benihakak/geekonomy` (no `main` subject on the deployer); **Workload Identity of the dump identities (N-1):** `for s in pg-backup pg-restore; do echo "$s: $(gcloud iam service-accounts get-iam-policy $s@boot-464019.iam.gserviceaccount.com --format=json \| jq -r '.bindings[]\|select(.role=="roles/iam.workloadIdentityUser")\|.members[]' \| sort \| tr '\n' ' ')"; done` → `pg-backup: serviceAccount:boot-464019.svc.id.goog[atlas-staging/pg-backup] …[atlas/pg-backup] …[content-studio/pg-backup]` (3) and `pg-restore: …[atlas-restore-test/pg-restore] …[atlas-staging/pg-restore] …[atlas/pg-restore] …[content-studio/pg-restore]` (4); **Workload Identity of the kradle runtime (Q-1 — the release KSA renders as `kradle-kradle`):** `for s in kradle-runtime-prod kradle-runtime-staging; do echo "$s: $(gcloud iam service-accounts get-iam-policy $s@boot-464019.iam.gserviceaccount.com --format=json \| jq -r '.bindings[]\|select(.role=="roles/iam.workloadIdentityUser")\|.members[]' \| sort \| tr '\n' ' ')"; done` → `kradle-runtime-prod: serviceAccount:boot-464019.svc.id.goog[kradle-org-a5c-ai/kradle-kradle] …[kradle/kradle-kradle]` (2) and `kradle-runtime-staging: …[kradle-org-staging-a5c-ai/kradle-kradle] …[kradle-staging/kradle-kradle]` (2) | as stated; no `attribute.ref` binding anywhere (`gcloud … get-iam-policy` for every SA \| grep -c attribute.ref = 0); `pg-backup` and `pg-restore` KSA sets are disjoint | output |
| X-B.10 | Deployer SA roles; foreign SA least privilege (review-04 N-1); dump-bucket identities split (review-05 N-1) | `gcloud projects get-iam-policy boot-464019 --flatten='bindings[].members' --format='table(bindings.role,bindings.members)' \| grep -E 'github-deployer\|geekonomy-deployer\|benchmarks-deployer\|github-livestack\|pg-backup\|pg-restore\|external-secrets'`; then the bucket policy: `gcloud storage buckets get-iam-policy gs://boot-464019-pg-dumps --format=json \| jq -r '.bindings[]\|"\(.role) \(.members[])"' \| grep -E 'pg-(backup\|restore)@'` | `github-deployer@`: `container.developer`; `geekonomy-deployer@`: `container.clusterViewer` and **no** `container.developer` and **no** `cloudsql.admin`; `geekonomy-terraform@`: `cloudsql.admin` (conditioned) and **no** `container.*`; `benchmarks-deployer@`: `firebasehosting.admin` only; `github-livestack@`: `aiplatform.user`; `external-secrets@`: `secretmanager.secretAccessor`; **`pg-backup@` and `pg-restore@`: no project-level role at all** (the first command prints no line for either); bucket policy: exactly `roles/storage.objectCreator serviceAccount:pg-backup@…` and `roles/storage.objectViewer serviceAccount:pg-restore@…` — `pg-restore@` has `storage.objectViewer` on this bucket and **nothing else anywhere** | `grep -c 'roles/container.developer.*geekonomy-deployer'` = 0; `grep -c 'roles/cloudsql.admin.*geekonomy-deployer@'` = 0; project policy `grep -c 'pg-restore@'` = 0; bucket policy prints exactly the two lines | output |
| X-B.11 | `kubectl auth can-i` matrix | `kubectl auth can-i create customresourcedefinitions --as=github-deployer@boot-464019.iam.gserviceaccount.com`; `kubectl auth can-i create deployments -n kradle --as=github-deployer@…`; `kubectl auth can-i create deployments -n geekonomy --as=geekonomy-deployer@…`; `kubectl auth can-i create deployments -n kradle --as=geekonomy-deployer@…`; `kubectl auth can-i list secrets -n atlas --as=geekonomy-deployer@…` | `yes`, `yes`, `yes`, `no`, `no` | exact (impersonation needs the operator's admin context) | output |
| X-B.12 | Platform addons Ready | `kubectl -n ingress-nginx rollout status deploy/ingress-nginx-controller; kubectl -n cert-manager rollout status deploy/cert-manager; kubectl -n external-secrets rollout status deploy/external-secrets; kubectl -n kyverno rollout status deploy/kyverno-admission-controller; kubectl get clusterissuer letsencrypt-production letsencrypt-staging -o jsonpath='{.items[*].status.conditions[0].status}'; kubectl get clustersecretstore gcp-secret-manager -o jsonpath='{.status.conditions[0].reason}'` | rollouts ok; `True True`; `Valid` | all | output |
| X-B.13 | Firebase site + ADC dry run (A8) — **partly manual** (a dispatch in a foreign repo by its owner) | `gcloud alpha firebase hosting sites list --project boot-464019 \| grep a5c-benchmarks` (or `terraform output firebase_required_dns_updates`); **manual:** the `MantisOS/babysitter-benchmarks` owner runs `publish-reports.yml` `workflow_dispatch` with `channel=preview` → `firebase hosting:channel:deploy` succeeds via WIF ADC | site exists; preview URL 200 | run `success`; if it fails on permissions, the added role is named in the PR and is not `roles/firebase.admin`; if it fails on ADC, D-15 alternative recorded in 03 §3.1 | run URL (evidence = run link + `curl -sI <preview url>` output) |
| X-B.14 | Budget + notification channel exist before any Vertex workflow | `gcloud billing budgets list --billing-account=$(gcloud billing projects describe boot-464019 --format='value(billingAccountName)' \| sed 's#billingAccounts/##') --format='value(displayName,amount.specifiedAmount.units)'` ; `gcloud alpha monitoring channels list --format='value(labels.email_address)'` | `a5c-boot-infra 500` (per D-19); `support@a5c.ai` | both | output |
| X-B.15 | Buckets for exports | `gcloud storage buckets describe gs://boot-464019-azure-archive --format='value(versioning.enabled,storageClass)'; gcloud storage buckets describe gs://boot-464019-pg-dumps --format='value(versioning.enabled,lifecycle.rule[0].action.type)'` | `True NEARLINE`; `True Delete` | both | output |
| X-B.16 | Secret Manager shells (no versions yet) | `gcloud secrets list --format='value(name)' \| grep -cE '^(atlas-(prod\|staging)-(github-oauth\|postgres)\|atlas-staging-webui-auth\|kradle-(prod\|staging)-(assistant-keys\|github-oauth\|test-auth\|jitsi-jwt))$'` | `13` | exact | output |
| X-B.17 | Backup for GKE plan | `gcloud beta container backup-restore backup-plans describe gitea-daily --location us-central1 --format='value(backupSchedule.cronSchedule,backupConfig.selectedNamespaces.namespaces,retentionPolicy.backupRetainDays)'` | `30 3 * * * kradle;kradle-staging 14` | exact | output |
| X-B.18 | Namespaces, quotas, LimitRanges, NetworkPolicies, geekonomy RoleBinding | `kubectl get ns atlas atlas-staging kradle kradle-staging gke-canary -o name \| wc -l; kubectl get resourcequota,limitrange -A --no-headers \| wc -l; kubectl get networkpolicy -A --no-headers \| wc -l; kubectl -n geekonomy get rolebinding geekonomy-deployer -o jsonpath='{.subjects[0].name}'` | `5`; ≥ 8; ≥ 4; `geekonomy-deployer@boot-464019.iam.gserviceaccount.com` | as stated | output |
| X-B.19 | Canary Ingress answers on the static IP | `curl -sk --resolve gke-canary.a5c.ai:443:<IP> https://gke-canary.a5c.ai/ -o /dev/null -w '%{http_code}'` | `200` | exact | output |
| X-B.20 | infra-seed module tag pinned | `grep -n 'k8s-cluster?ref=k8s-cluster-gcp-v2' terraform/modules/gcp-host/gke.tf` (in `a5c-ai/infra`) | one match | exit 0 | output |
| X-B.21 | `github-terraform@` narrowed after first CI apply | `gcloud projects get-iam-policy boot-464019 --flatten='bindings[].members' --filter='bindings.members:github-terraform@' --format='value(bindings.role)' \| grep -c '^roles/owner$'` | `0` (after the narrowing PR) | exact | output |
| X-B.22 | kubectl provider is the maintained fork (N-5) | `grep -rn 'alekc/kubectl' terraform/modules/gcp-addons/versions.tf; grep -rn 'gavinbunney' terraform/ \| wc -l` | match; `0` | both | output |

## Phase C — GitHub configuration

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-C.1 | Org variables exist (9 — there is no `GKE_INGRESS_IP`) | `gh api orgs/a5c-ai/actions/variables --paginate -q '.variables[].name' \| grep -cE '^(GCP_PROJECT_ID\|GCP_PROJECT_NUMBER\|GCP_REGION\|GCP_WORKLOAD_IDENTITY_PROVIDER\|GCP_SERVICE_ACCOUNT\|GCP_LIVESTACK_SERVICE_ACCOUNT\|GCP_ARTIFACT_REGISTRY\|GKE_CLUSTER_NAME\|GKE_LOCATION)$'; gh api orgs/a5c-ai/actions/variables --paginate -q '.variables[].name' \| grep -c '^GKE_INGRESS_IP$'` | `9`; `0` | exact | output |
| X-C.2 | Org variable values match terraform outputs | for each: `gh api orgs/a5c-ai/actions/variables/<NAME> -q .value` vs `terraform -chdir=terraform/envs/gcp-boot/platform output -raw <output>` (`GCP_PROJECT_ID`=`boot-464019`, `GCP_PROJECT_NUMBER`=`project_number`, `GCP_REGION`=`us-central1`, `GCP_WORKLOAD_IDENTITY_PROVIDER`=`wif_provider_a5c_ai`, `GCP_SERVICE_ACCOUNT`=`github-deployer@boot-464019.iam.gserviceaccount.com`, `GCP_LIVESTACK_SERVICE_ACCOUNT`=`github-livestack@…`, `GCP_ARTIFACT_REGISTRY`=`us-central1-docker.pkg.dev/boot-464019/a5c-images`, `GKE_CLUSTER_NAME`=`a5c-boot-gke`, `GKE_LOCATION`=`us-central1-a`) | equal ×9 | `diff` empty for each | table in `raw/verify/X-C.2.txt` (values are not secret) |
| X-C.3 | infra repo variables (5) | `gh api repos/a5c-ai/infra/actions/variables -q '.variables[].name' \| grep -cE '^(GCP_PROJECT_ID\|GCP_WORKLOAD_IDENTITY_PROVIDER\|GCP_TERRAFORM_SERVICE_ACCOUNT\|GCP_TERRAFORM_PLAN_SERVICE_ACCOUNT\|TF_STATE_BUCKET)$'` | `5` | exact | output |
| X-C.4 | babysitter provider vars (only after commit A.9 is pushed to `staging`) | `gh api repos/a5c-ai/babysitter/actions/variables/A5C_PROVIDER_NAME -q .value; …/A5C_SELECTED_CLI_COMMAND; …/A5C_SELECTED_MODEL` | `vertex` / `gemini-cli` / model per D-7 | exact | output |
| X-C.5 | Foreign repo variables (set by owners) | geekonomy: `gh api repos/Benihakak/geekonomy/actions/variables -q '.variables[].name'` (7 names incl. `GCP_WORKLOAD_IDENTITY_PROVIDER` ending `/providers/external`, `GCP_ARTIFACT_REGISTRY` = `…/geekonomy`); benchmarks: 3 names | as 05 C.2 | counts 7 / 3 and the provider value ends in `/providers/external` | output |
| X-C.6 | Environment reviewers | `gh api repos/a5c-ai/babysitter/environments/kradle-production -q '.protection_rules[]\|select(.type=="required_reviewers")\|.reviewers[].reviewer.login'` and `atlas-production` | owner login | non-empty for both | output |
| X-C.7 | No GitHub **secret** was created by Phase C, and exactly one was updated | `gh api orgs/a5c-ai/actions/secrets -q '.secrets[]\|[.name,.updated_at]\|@tsv' \| awk -v d=<phase-C-date> '$2>=d'`; `gh api repos/a5c-ai/babysitter/actions/secrets -q '.secrets[]\|[.name,.updated_at]\|@tsv' \| awk -v d=<phase-C-date> '$2>=d'` | org: empty; repo: exactly one line, `KRADLE_TEST_AUTH_SECRET <phase-C-date>…` | no org secret touched; the only repo secret updated on/after the Phase C date is `KRADLE_TEST_AUTH_SECRET` (X-C.10) — nothing at G.3 | output |
| X-C.10 | `KRADLE_TEST_AUTH_SECRET` repo secret written in Phase C from Secret Manager via stdin (N-3; moved here from G.3) | `gh api repos/a5c-ai/babysitter/actions/secrets/KRADLE_TEST_AUTH_SECRET -q .updated_at` — after `gcloud secrets versions access latest --secret kradle-staging-test-auth \| gh secret set KRADLE_TEST_AUTH_SECRET --repo a5c-ai/babysitter` (05 C.0) | the Phase C date, **before** the Phase A push (`git log -1 --format=%cI origin/staging` of the Phase A push is later) | date ordering holds | output (a date, never a value) |
| X-C.11 | The two test-auth shells hold the **same** value (one value, both envs, one repo secret) | `for s in kradle-staging-test-auth kradle-prod-test-auth; do gcloud secrets versions access latest --secret $s \| sha256sum \| cut -c1-16; done` (the value only ever crosses a pipe into `sha256sum`) | two identical 16-hex prefixes | equal | output (hash prefixes only) |
| X-C.8 | Azure names still present (they must survive until H.4); `A5C_AGENT_GITHUB_TOKEN` kept on purpose | `gh api orgs/a5c-ai/actions/secrets -q '.secrets[].name' \| grep -c AZURE; gh api orgs/a5c-ai/actions/secrets -q '.secrets[].name' \| grep -c '^A5C_AGENT_GITHUB_TOKEN$'` | ≥ 3; `1` | non-zero until Phase H; `1` always | output |
| X-C.9 | OAuth client secrets regenerated **once**, in Phase C (never again at G.3) | `for s in atlas-prod-github-oauth atlas-staging-github-oauth kradle-prod-github-oauth kradle-staging-github-oauth; do echo "$s $(gcloud secrets versions list $s --filter=state=ENABLED --format='value(name)' \| wc -l)"; done` — recorded now and re-run at X-G.3 | each `1` now; **still `1`** at G.3 | counts only; unchanged through Phase G | output (counts only) |

## Phase D — Redeploy from source

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-D.0 | The 7 staging Secret Manager values entered in Phase C.0 (never printed) — **before the Phase A push** | `for s in atlas-staging-github-oauth atlas-staging-postgres atlas-staging-webui-auth kradle-staging-assistant-keys kradle-staging-github-oauth kradle-staging-test-auth kradle-staging-jitsi-jwt; do echo "$s $(gcloud secrets versions list $s --filter=state=ENABLED --format='value(name)' \| wc -l)"; done` | each `≥ 1` | all seven | output (counts only) |
| X-D.0a | The 6 prod Secret Manager values entered in Phase C.0 — **before D.3/D.4** (the `main` promotion) | `for s in atlas-prod-github-oauth atlas-prod-postgres kradle-prod-assistant-keys kradle-prod-github-oauth kradle-prod-test-auth kradle-prod-jitsi-jwt; do echo "$s $(gcloud secrets versions list $s --filter=state=ENABLED --format='value(name)' \| wc -l)"; done` | each `≥ 1` | all six | output (counts only) |
| X-D.0b | The staging run was triggered by the Phase A push (not a PR merge) | `gh run list --workflow publish.yml --branch staging -L1 --json event,headSha -q '.[0] \| [.event,.headSha] \| @tsv'`; `git rev-parse HEAD` | `push <sha>`; same sha | equal | output |
| X-D.1 | `Deploy Atlas WebUI To GKE` (staging) | `gh run list --workflow publish.yml --branch staging -L1 --json databaseId,conclusion`; `gh run view <id> --json jobs -q '.jobs[]\|select(.name=="Deploy Atlas WebUI To GKE")\|.conclusion'` | `success` | exact | run URL |
| X-D.1a | atlas-webui image in AR by SHA | `gcloud artifacts docker images list us-central1-docker.pkg.dev/boot-464019/a5c-images/atlas-webui --filter="tags:$(git rev-parse origin/staging)" --format='value(tags)'` | the sha | non-empty | output |
| X-D.1b | atlas-staging objects | `kubectl -n atlas-staging get externalsecret -o jsonpath='{range .items[*]}{.metadata.name}={.status.conditions[0].reason}{"\n"}{end}'; kubectl -n atlas-staging rollout status statefulset/atlas-postgres; kubectl -n atlas-staging get pvc data-atlas-postgres-0 -o jsonpath='{.status.phase} {.spec.storageClassName}'; kubectl -n atlas-staging get job atlas-webui-db-init -o jsonpath='{.status.succeeded}'; kubectl -n atlas-staging rollout status deploy/atlas-webui; kubectl -n atlas-staging get cronjob atlas-postgres-backup -o jsonpath='{.spec.schedule}'; for s in pg-backup pg-restore; do kubectl -n atlas-staging get sa $s -o jsonpath="$s={.metadata.annotations.iam\.gke\.io/gcp-service-account}{'\n'}"; done` | 3× `SecretSynced` (`atlas-auth`, `atlas-postgres`, `atlas-webui-auth` — staging has the basic-auth one); ok; `Bound standard-rwo`; `1`; ok; `0 3 * * *`; `pg-backup=pg-backup@boot-464019.iam.gserviceaccount.com` and `pg-restore=pg-restore@boot-464019.iam.gserviceaccount.com` (N-1: both KSAs exist from the kustomize apply) | all | output |
| X-D.1c | atlas-staging serves (pre-DNS) | `curl -sk --resolve atlas-staging.a5c.ai:443:<IP> https://atlas-staging.a5c.ai/ -o /dev/null -w '%{http_code}'` | `200` | exact | output |
| X-D.2 | `Deploy Kradle To GKE` (staging) | as X-D.1 with job name `Deploy Kradle To GKE` | `success` | exact | run URL |
| X-D.2a | four images by SHA | `for i in kradle-controller kradle-web adapters-agent jitsi-agent-sidecar; do gcloud artifacts docker images list us-central1-docker.pkg.dev/boot-464019/a5c-images/$i --filter="tags:$(git rev-parse origin/staging)" --format='value(package)'; done \| wc -l` | `4` | exact | output |
| X-D.2b | kradle-staging objects | `kubectl -n kradle-staging get externalsecret -o jsonpath='{range .items[*]}{.status.conditions[0].reason}{"\n"}{end}' \| sort -u; for d in kradle-kradle-api kradle-kradle-controllers kradle-kradle-webhook-worker kradle-kradle-web kradle-kradle-gitea kradle-jitsi-subchart-web kradle-jitsi-subchart-jicofo kradle-jitsi-subchart-jvb; do kubectl -n kradle-staging rollout status deploy/$d --timeout=120s; done; kubectl -n kradle-staging rollout status sts/kradle-prosody; kubectl -n kradle-staging get pvc -o custom-columns=N:.metadata.name,P:.status.phase,C:.spec.storageClassName` | `SecretSynced` only; 8 rollouts ok; prosody ok; gitea + prosody PVCs `Bound standard-rwo` | all | output |
| X-D.2c | Env-prefixed org namespace, no shared one (bootstrap set of staging = exactly one org namespace) | `kubectl get ns -o name \| grep -cE '^namespace/kradle-org-staging-'; kubectl get ns -o name \| grep -E 'kradle-org-staging-a5c-ai'; kubectl get ns kradle-org-a5c-ai 2>&1 \| grep -c NotFound` (before the `main` promotion; after it the prod release creates only `kradle-org-a5c-ai`) | `1`; match; `1` | all | output |
| X-D.2d | CRDs installed = exactly what the chart ships at the deployed sha | `SHA=$(git rev-parse origin/staging); EXPECTED=$(git ls-tree --name-only "$SHA" packages/kradle/charts/crds/ \| xargs -I{} git show "$SHA":{} \| grep -c 'kind: CustomResourceDefinition'); echo "expected=$EXPECTED"; kubectl get crd -o name \| grep -c 'kradle.a5c.ai'` (verified to print `106` at `57b13a4c5`; with the sha checked out it is simply `grep -h 'kind: CustomResourceDefinition' packages/kradle/charts/crds/*.yaml \| wc -l`; for prod use the D-24 release tag instead of `origin/staging`) | `expected=106` at `57b13a4c5` (28+36+5+14+4+4+4+11 across the 8 CRD files) and the cluster count **equals `$EXPECTED`** — never a hard-coded number (01's `99` is the older running AKS release `21ded0e5`) | cluster count = chart count at the deployed sha | output |
| X-D.2e | Workload Identity annotation | `kubectl -n kradle-staging get sa kradle-kradle -o jsonpath='{.metadata.annotations.iam\.gke\.io/gcp-service-account}'` (SA name = `{{ include "kradle.fullname" . }}` = `kradle-kradle` for release `kradle`, `templates/serviceaccount.yaml:5`, `_helpers.tpl:5-11`) | `kradle-runtime-staging@boot-464019.iam.gserviceaccount.com` | exact | output |
| X-D.2f | In-cluster health | `kubectl -n kradle-staging exec deploy/kradle-kradle-web -- wget -qO- 'http://kradle-kradle-api:80/healthz?quick=1' \| jq -e '.ok == true or .health.kubernetes == "ok"'` | `true` | exit 0 | output |
| X-D.2g | Hosts pre-DNS | `for h in kradle-staging.a5c.ai gitea.kradle-staging.a5c.ai; do curl -sk --resolve $h:443:<IP> https://$h/ -o /dev/null -w "$h %{http_code}\n"; done` | `307`, `200` | exact | output |
| X-D.2h | `Kradle E2E Smoke Test` red **for the expected reason** before G.2: the public host still resolves to the AKS release, whose api holds the Azure-era test-auth value, while the repo secret holds the C.0 value → the authenticated step now exits 1 (A.4 step 10; N-3); no DNS gate exists in the workflow | `gh run view <staging run> --json jobs -q '.jobs[]\|select(.name=="Kradle E2E Smoke Test")\|.conclusion'`; `gh run view <staging run> --log --job <job id> \| grep -cE 'Test session endpoint unreachable or rejected the secret'`; `gh run view <staging run> --log --job <job id> \| grep -c '::warning::Test session endpoint not available'`; `dig +short kradle-staging.a5c.ai @ns41.domaincontrol.com`; `grep -c 'GKE_INGRESS_IP\|dig +short' .github/workflows/publish.yml`; `grep -c 'Test session endpoint not available' .github/workflows/publish.yml` | `failure`; `1`; `0`; `135.234.117.214`; `0`; `0` | the failure is the authenticated smoke against the Azure-served public host (loud `::error::`, not a swallowed `::warning::`), DNS still points at Azure, the workflow contains neither a DNS gate nor the old warn-and-exit-0 branch. Green after G.2 is X-G.2 | run URL + output |
| X-D.2i | `G0-RT Jitsi E2E` via WIF | `gh workflow run g0-rt-jitsi-e2e.yml --ref staging && gh run watch` | `success`; log has no `KUBE_CONFIG` | conclusion `success` | run URL |
| X-D.3 | atlas prod deploy at the release tag | `gh run view <main run> --json jobs -q '.jobs[]\|select(.name=="Deploy Atlas WebUI To GKE")\|.conclusion'`; `kubectl -n atlas get deploy atlas-webui -o jsonpath='{.spec.template.spec.containers[0].image}'` | `success`; `…/atlas-webui:<release sha>` where `<release sha>` = `git rev-parse <D-24 tag>` | both | run URL |
| X-D.3a | atlas prod objects (as X-D.1b with `-n atlas`) | same commands with `-n atlas` | as X-D.1b **except** the ExternalSecrets: **2×** `SecretSynced` (`atlas-auth`, `atlas-postgres` — prod has no `atlas-webui-auth`, N-5); both KSAs annotated | all | output |
| X-D.4 | kradle prod deploy at the release tag | as X-D.3 for `Deploy Kradle To GKE`; images tagged `<release sha>` | `success` | both | run URL |
| X-D.4a | kradle prod objects (as X-D.2b/e/f with `-n kradle`, SA `kradle-runtime-prod@`) **plus the chart bootstrap set — exactly what the chart and the api create at the deployed sha (N-2; verified in `templates/gitea.yaml:89-115`, `templates/gitea-token-job.yaml`, `http-server.js:58`, `auth.js:140-150`, `repository-reconciler.js`)** | same commands; then, **before any login** (X-F.5/X-F.6 come later): (1) `for k in organizations users repositories; do echo "$k=$(kubectl -n kradle get $k.kradle.a5c.ai --no-headers 2>/dev/null \| wc -l)"; done; echo "org-users=$(kubectl -n kradle-org-a5c-ai get users.kradle.a5c.ai --no-headers 2>/dev/null \| wc -l)"`; (2) `kubectl -n kradle get deploy kradle-kradle-api -o jsonpath='{.spec.template.spec.containers[0].env[?(@.name=="KRADLE_ADMIN_ORG")].value} {.spec.template.spec.containers[0].env[?(@.name=="KRADLE_ADMIN_USERNAME")].value}'`; (3) `kubectl -n kradle get secret kradle-gitea-agent-token --template '{{len .data.token}}'` (length only — the value never leaves the API server response); (4) `kubectl -n kradle get job kradle-kradle-gitea-token-provision 2>&1 \| grep -c NotFound; helm -n kradle status kradle -o json \| jq -r .info.status` (the hook Job is deleted on success by `helm.sh/hook-delete-policy: hook-succeeded`; a failed hook fails `helm --wait` and leaves the Job behind); (5) `kubectl -n kradle exec deploy/kradle-kradle-gitea -- sh -c 'curl -sf -o /dev/null -w "%{http_code} " -u "$GITEA_ADMIN_USER:$GITEA_ADMIN_PASSWORD" http://localhost:3000/api/v1/orgs/a5c-ai; curl -sf -u "$GITEA_ADMIN_USER:$GITEA_ADMIN_PASSWORD" http://localhost:3000/api/v1/repos/a5c-ai/agent-sandbox \| grep -o "\"full_name\":\"a5c-ai/agent-sandbox\""'` (the repo is **private**, so the check authenticates with the admin credentials that are env of the gitea container — they never leave the pod) | same; (1) `organizations=0 users=0 repositories=0 org-users=0` — **zero** CRs: nothing creates them at start-up; (2) `a5c-ai tmuskal`; (3) `> 0`; (4) `1` and `deployed`; (5) `200 "full_name":"a5c-ai/agent-sandbox"` | all — this **is** the whole expected kradle prod state before the first login (no import); the first `User` appears only at login (X-F.5a), the first `Repository`/`Organization` only through the API (X-F.11) | output (no secret values) |
| X-D.4b | Prod org namespaces = the bootstrap set (one), with NetworkPolicy; the AKS runtime orgs do not exist | `kubectl get ns -o name \| grep -E '^namespace/kradle-org-' \| grep -vc staging` (the `-c` sits on the **last** grep only — N-5: `grep -cE` first would collapse the list to a number before the staging filter); `kubectl get ns kradle-org-a5c-ai -o name; kubectl -n kradle-org-a5c-ai get networkpolicy --no-headers \| wc -l; for n in default knack commander-verify; do kubectl get ns kradle-org-$n 2>&1 \| grep -c NotFound; done` | `1`; `namespace/kradle-org-a5c-ai`; ≥ 1; `1 1 1` | all | output |
| X-D.4c | atlasBaseUrl per env (C17 fixed) | `kubectl -n kradle get deploy kradle-kradle-web -o jsonpath='{.spec.template.spec.containers[0].env[?(@.name=="ATLAS_BASE_URL")].value}'; same `-n kradle-staging` | `http://atlas-webui.atlas.svc.cluster.local`; `http://atlas-webui.atlas-staging.svc.cluster.local` | exact | output |
| X-D.5 | `deploy_staging_cloud` still dormant | `gh run view <staging run> --json jobs -q '.jobs[]\|select(.name=="Deploy Cloud")\|.conclusion'` | `skipped` | exact | run URL |
| X-D.6 | geekonomy (`deploy-gke.yml`) | `gh run list --repo Benihakak/geekonomy --workflow deploy-gke.yml -L1 --json conclusion`; `for i in web worker transcriber migrator; do gcloud artifacts docker images list us-central1-docker.pkg.dev/boot-464019/geekonomy/geekonomy-$i --format='value(package)' \| head -1; done \| wc -l`; `kubectl -n geekonomy rollout status deploy/geekonomy-web deploy/geekonomy-worker deploy/geekonomy-transcriber deploy/geekonomy-redis` (Deployment names are the chart's `<fullname>-<component>` for release `geekonomy`; `geekonomy-redis` is `redis-deployment.yaml`)`; kubectl -n geekonomy get pod -l app.kubernetes.io/name=geekonomy-transcriber -o jsonpath='{.items[0].spec.nodeSelector}'; kubectl -n geekonomy get pvc -o jsonpath='{.items[0].status.phase}'; curl -sk --resolve geekonomy.a5c.ai:443:<IP> https://geekonomy.a5c.ai/ -o /dev/null -w '%{http_code}'`; `gcloud sql instances describe geekonomy-pg --format='value(databaseVersion,settings.tier,ipAddresses[0].type,settings.ipConfiguration.sslMode)'` | `success`; `4`; rollouts ok; `{"a5c.ai/pool":"agents"}`; `Bound`; `200`; `POSTGRES_16 db-f1-micro PRIVATE ENCRYPTED_ONLY` | all | outputs |
| X-D.7 | benchmarks (`publish-reports.yml`) | `gh run list --repo MantisOS/babysitter-benchmarks --workflow publish-reports.yml -L1 --json conclusion`; `curl -sI https://a5c-benchmarks.web.app/tb21-paper.html -o /dev/null -w '%{http_code}'` | `success`; `200` | both | outputs |
| X-D.8 | content-studio (`content-studio-deploy.yml`, D-8) | `gh run list --repo a5c-ai/company --workflow content-studio-deploy.yml -L1 --json conclusion`; `kubectl -n content-studio rollout status deploy/content-studio sts/content-studio-postgres; kubectl -n content-studio get pvc -o jsonpath='{.items[0].spec.storageClassName}'; kubectl -n content-studio get cronjob -o name; curl -sk --resolve studio.a5c.ai:443:<IP> https://studio.a5c.ai/ -o /dev/null -w '%{http_code}'` | `success`; ok; `standard-rwo`; `cronjob…/content-studio-postgres-backup`; `307` | all | outputs |
| X-D.9 | osb source PR merged, not deployed (D-14) | `gh pr list --repo a5c-ai/osb --state merged --search 'GKE' --json number \| jq length`; `grep -c 'ghcr-pull-secret' .github/workflows/cd.yml` (in osb) ; `kubectl get ns osb-staging 2>&1 \| grep -c NotFound` | ≥ 1; `0`; `1` | all | outputs |
| X-D.10 | askExpertQuestion (D-9 default) | `test -f docs/migration/azure-to-gcp/decommission-notes/askExpertQuestion.md`; `kubectl get ns bpx 2>&1 \| grep -c NotFound` (unless D-9 = keep, then the bpx rows mirror X-D.6 with `bpx-pg`) | file; `1` | both | file |
| X-D.11–13 | claude-web, app, hub decommission notes | `ls docs/migration/azure-to-gcp/decommission-notes/{claude-web,app,hub}.md`; each contains sections `What runs`, `What is exported (E.0 row)`, `What is deleted (H.2 step)`, `No code change` | 3 files | `grep -c '^## ' <file>` ≥ 4 each | files |
| X-D.14 | No pod anywhere with pull secrets or missing requests | `kubectl get pods -A -o json \| jq '[.items[]\|select(.metadata.namespace!="kube-system")\|select(.spec.imagePullSecrets!=null)]\|length'; kubectl get pods -A -o json \| jq '[.items[]\|select(.metadata.namespace!="kube-system")\|.spec.containers[]\|select(.resources.requests==null)]\|length'` | `0`; `0` | exact | output |
| X-D.15 | Azure untouched by Phase D (Deployment **count**, not revision — the docs commit and any Phase A revert redeploy the unchanged Azure workflow on AKS, which bumps revisions but not counts; 05 ground rule 3b) | `az group list --query 'length(@)'; kubectl --context aks-hub-development-westus3-v2 get deploy -A --no-headers \| wc -l; kubectl --context aks-hub-development-westus3-v2 get ns --no-headers \| wc -l` | `33`; same Deployment count as `raw/k8s/` from 01; `32` | equal counts | output |

## Phase E — Data migration (one row per stateful item; "confirmed no export" rows included)

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-E.1 | atlas-staging dump exists + integrity | `gcloud storage ls -L gs://boot-464019-pg-dumps/atlas-staging/ \| grep -E 'atlas-staging-.*\.dump$' \| tail -1; diff <(gcloud storage hash --hex --skip-md5 raw/data/atlas-staging-$DATE.dump \| grep -i crc32c_hash \| awk '{print $NF}') <(gcloud storage hash --hex --skip-md5 gs://boot-464019-pg-dumps/atlas-staging/atlas-staging-$DATE.dump \| grep -i crc32c_hash \| awk '{print $NF}')` (CRC32C on both sides — `gcloud storage hash` has no `--sha256`; MD5 is absent on parallel-composite uploads); optionally `gcloud storage cat gs://boot-464019-pg-dumps/atlas-staging/atlas-staging-$DATE.dump \| sha256sum \| cut -d' ' -f1` vs the local `.sha256` sidecar; `pg_restore --list atlas-staging-$DATE.dump \| wc -l` | object present; empty diff (CRC32C equal; sidecar SHA-256 equal if read back); count > 0 | all | `raw/data/atlas-staging-$DATE.*` |
| X-E.2 | atlas prod dump + wave-0 counts of **both** DBs (D-23) | as X-E.1 with `atlas/`; `diff <(cut -f1,2 raw/data/atlas-$DATE.counts.tsv) <(cut -f1,2 raw/data/atlas-staging-$DATE.counts.tsv)` | CRC32C equal (as X-E.1); diff output recorded (equal or not) | recorded and shown to the owner at OB-2 | `raw/data/atlas-*.counts.tsv`, diff |
| X-E.3 | atlas prod import verified on GKE (the restore Job ran as KSA `pg-restore`) | `kubectl -n atlas get job atlas-postgres-restore -o jsonpath='{.spec.template.spec.serviceAccountName} {.status.succeeded}'`; `kubectl -n atlas exec -i atlas-postgres-0 -- sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -Atf /dev/stdin' < raw/data/rowcount.sql > raw/data/atlas-gke-$DATE.counts.tsv; diff raw/data/atlas-gke-$DATE.counts.tsv raw/data/<seed>-$DATE.counts.tsv` (`<seed>` per D-23; `-i` forwards stdin, the single-quoted `sh -c` expands `POSTGRES_USER`/`POSTGRES_DB` **inside** the pod — N-4) | `pg-restore 1`; empty diff | both | files |
| X-E.4 | content-studio dump (+ import if D-8) | as X-E.1 with `content-studio/`; if D-8: restore Job `1` + counts diff empty | CRC32C equal (as X-E.1) | all | files |
| X-E.5 | aeq Azure PG dump (both formats) | `gcloud storage ls gs://boot-464019-pg-dumps/aeq/ \| grep -cE 'aeq-.*\.(dump\|sql\.gz)$'`; CRC32C check as X-E.1 (`gcloud storage hash --hex --skip-md5` local vs `gs://`) for both objects | `2`; CRC32C equal ×2 | both | files |
| X-E.6 | hub in-cluster PG dump | `gcloud storage ls -L gs://boot-464019-azure-archive/hub-postgres/ \| grep -c 'sql.zst'`; `zstd -t hub-inpg-$DATE.sql.zst` | `1`; ok | both | files |
| X-E.7 | hub Azure PG (`psql-hub-development-westus3-v2`) dumped **or** explicitly skipped | `gcloud storage ls gs://boot-464019-azure-archive/hub-azure-pg/ \| wc -l` or `grep -c 'E.0.6 skipped' docs/migration/azure-to-gcp/decisions.md` | ≥ 1 or `1` | one of the two | file |
| X-E.8 | geekonomy PG dump + Cloud SQL import (D-13) | `gcloud storage ls gs://boot-464019-pg-dumps/geekonomy/ \| grep -c 'sql.gz$'`; `gcloud sql operations list --instance geekonomy-pg --filter='operationType=IMPORT' --format='value(status)' \| head -1`; per-table counts: `kubectl -n geekonomy run psql --rm -i --restart=Never --image=postgres:16-alpine --overrides='{"spec":{"containers":[{"name":"psql","image":"postgres:16-alpine","stdin":true,"command":["sh","-c","psql \"$DATABASE_URL\" -Atf -"],"envFrom":[{"secretRef":{"name":"geekonomy-secrets"}}]}]}}' < raw/data/rowcount.sql \| diff - raw/data/geekonomy-$DATE.counts.tsv` (`kubectl run` accepts only `--env` and `--overrides`, no secret-wide env flag — the X-E.23 `--overrides` `envFrom.secretRef` form; `"stdin":true` keeps the `-i` stdin forwarding on the overridden container; `DATABASE_URL` is consumed inside the pod, never printed) | ≥ 1; `DONE`; empty diff | all | files |
| X-E.8a | geekonomy Redis: confirmed no export; queue refills | `kubectl -n geekonomy logs deploy/geekonomy-worker --since=10m \| grep -ci 'rss'` | ≥ 1 | non-zero | output |
| X-E.9 | a5c-app PG ×2 dumps | `gcloud storage ls gs://boot-464019-azure-archive/a5c-app/ \| grep -c 'sql.zst$'`; `zstd -t` both | `2`; ok | both | files |
| X-E.10 | Terraform state archive complete (blob-for-blob) | `az storage blob list --account-name a5ctfstorage -c tf-main --auth-mode login --query '[].name' -o tsv \| sort > /tmp/az.txt; gcloud storage ls -r gs://boot-464019-azure-archive/tfstate/$DATE/tf-main/** \| sed 's#.*/tf-main/##' \| sort > /tmp/gcs.txt; diff /tmp/az.txt /tmp/gcs.txt` (same for `tfstate` container; MD5 per blob compared) | empty diff ×2; MD5s equal | all | files |
| X-E.11 | **trial-telemetry** verified decompressed (N-3) | `gcloud storage ls gs://boot-464019-azure-archive/trial-telemetry/ \| wc -l`; the loop from 05 E.0.10 over every `BULK-MANIFEST.tsv` row (`zstd -d \| sha256sum` = manifest sha) | `180`; loop exits 0 with `MISMATCH` count 0 | both | `raw/data/trial-telemetry-verify.txt` |
| X-E.12 | Repo pointer PR merged (benchmarks) | `gh api repos/MantisOS/babysitter-benchmarks/contents/data/trial-evidence/README.md -q .content \| base64 -d \| grep -c 'gs://boot-464019-azure-archive/trial-telemetry'` | ≥ 1 | non-zero | output |
| X-E.13 | hub blobs, litellm shares + `$web`, mantis `evidence`, `mantisb131bfc8` `$web` (D-26), `a5cstatic` `$web` exported (counts) | for each `<account>/<container>`: `az storage blob list --account-name <acct> -c <c> --auth-mode login --query 'length(@)'` vs `gcloud storage ls -r gs://boot-464019-azure-archive/<acct>/<c>/** \| wc -l`; shares: local `find \| wc -l` vs GCS count | equal per container/share | all equal | `raw/data/exports.tsv` |
| X-E.14 | AzureML shares + hub 1 TiB share: exported only if D-12 says so | `grep -E 'D-12' docs/migration/azure-to-gcp/decisions.md`; if included: count check as X-E.13 (Archive class: `gcloud storage ls -L … \| grep -c ARCHIVE`) | decision line; counts | consistent with D-12 | file |
| X-E.15 | litellm PG ×2 dumps (kept forever) | `gcloud storage ls gs://boot-464019-azure-archive/litellm/ \| grep -cE 'litellm.*\.dump$'`; `pg_restore --list` count each | ≥ 3 (`litellm-pgdb`: 2 dbs, `litellm-pgdb2`: 1) | as stated | files |
| X-E.16 | kradle CRs (prod + staging): **confirmed no export, no import** (owner 2026-09-16, supersedes D-16/D-21) — the "no import" evidence is the empty CR state of a fresh bootstrap (N-2) | `grep -cE '^no-export: kradle-(prod\|staging)-crs' docs/migration/azure-to-gcp/decisions.md`; `gcloud storage ls gs://boot-464019-pg-dumps/ \| grep -cE 'kradle(-staging)?/'`; **before X-F.11:** `for ns in kradle kradle-staging; do echo "$ns orgs=$(kubectl -n $ns get organizations.kradle.a5c.ai --no-headers 2>/dev/null \| wc -l)"; done; echo "repos-all=$(kubectl get repositories.kradle.a5c.ai -A --no-headers 2>/dev/null \| wc -l)"` (`Organization` CRs live in the release namespace; API-created `Repository` CRs live in the **org** namespace — `http-server.js:76` scopes `POST /api/orgs/<org>/repositories` to `orgNamespaceName(org)` — hence the cluster-wide count); **after X-F.11:** the same commands | `2`; `0` (no kradle prefix in the dumps bucket); before: `kradle orgs=0`, `kradle-staging orgs=0`, `repos-all=0` (no `Organization`, no `Repository` anywhere — the chart bootstraps none; the AKS orgs `default`/`knack`/`commander-verify` never appear); after: `kradle-staging orgs=1` (the one X-F.11 created), `kradle orgs=0`, `repos-all=1` (X-F.11's staging `Repository` in `kradle-org-staging-a5c-ai`; its prod `Repository` was deleted again after `Ready`) | all | file + output |
| X-E.17 | *(deleted 2026-09-16 — CR import dry-run/apply/verify belonged to the CR lane)* | — | — | — | — |
| X-E.18 | *(re-pointed 2026-09-16, rev 3 — N-2: a fresh bootstrap has **no** `Repository` CR; the gitea sandbox repo is created by gitea's postStart hook through the gitea API and `repository-reconciler.js` is CR → gitea only, so nothing here can be `Ready`. The reconcile loop is proven **only** by X-F.11, in both envs.)* | see X-F.11 | — | — | — |
| X-E.19 | *(deleted 2026-09-16 — staging CR export "for the record" belonged to the CR lane)* | — | — | — | — |
| X-E.20 | Gitea: no export (taken 2026-09-16 with the kradle decision; D-27 recorded as taken) | `grep -cE '^\| D-27 .*(taken\|no gitea export)' docs/migration/azure-to-gcp/decisions.md; grep -c '^no-export: gitea' docs/migration/azure-to-gcp/decisions.md` | `1`; `1` | both | file |
| X-E.21 | Confirmed **no export**: `hub-development/hub-redis-pvc`, `app*/data-a5c-app-redis-0`, `staging` PG/Redis (no PVC), `kradle*/prosody-data-*` (Pending), `aeq/aeq-data-pvc` (Pending) | `kubectl --context aks-hub-development-westus3-v2 get pvc -A -o custom-columns=NS:.metadata.namespace,N:.metadata.name,S:.status.phase \| grep -E 'redis\|prosody\|aeq-data'` | list shows the items; decisions.md lines `no-export: <item>` for each | 5 lines | file |
| X-E.22 | Backup acceptance — pg_dump CronJob (as `pg-backup`, write-only) → restore into a scratch namespace (as **`pg-restore`**, read-only; N-1) | 1. dump: `kubectl -n atlas-staging create job --from=cronjob/atlas-postgres-backup backup-manual-$DATE; kubectl -n atlas-staging wait --for=condition=complete job/backup-manual-$DATE --timeout=600s; OBJ=$(gcloud storage ls gs://boot-464019-pg-dumps/atlas-staging/ \| grep '\.dump$' \| tail -1)`. 2. scratch namespace + identities: `kubectl create ns atlas-restore-test; kubectl kustomize packages/atlas/webui/deploy/base \| yq 'select(.kind=="ServiceAccount" and .metadata.name=="pg-restore")' \| kubectl -n atlas-restore-test apply -f -` (the KSA from **`pg-restore-sa.yaml`**, annotated `pg-restore@…`; its Workload Identity binding `[atlas-restore-test/pg-restore]` exists from B.2 `iam.tf` — X-B.9; **no** `pg-backup` KSA is created here). 3. DB credentials from Secret Manager through ESO, never copied from another namespace: `kubectl kustomize packages/atlas/webui/deploy/overlays/atlas-staging \| yq 'select(.kind=="ExternalSecret" and .metadata.name=="atlas-postgres") \| .metadata.namespace="atlas-restore-test"' \| kubectl apply -f -; kubectl -n atlas-restore-test wait --for=condition=Ready externalsecret/atlas-postgres --timeout=120s` (same `atlas-staging-postgres` remote key → the restored DB accepts the dump's role password). 4. DB: `kubectl kustomize packages/atlas/webui/deploy/base \| yq 'select((.kind=="Service" or .kind=="StatefulSet") and .metadata.name=="atlas-postgres")' \| kubectl -n atlas-restore-test apply -f -; kubectl -n atlas-restore-test rollout status statefulset/atlas-postgres --timeout=300s`. 5. restore: `export DUMP_OBJECT=$OBJ; envsubst '${DUMP_OBJECT}' < packages/atlas/webui/deploy/base/restore-job.yaml \| kubectl -n atlas-restore-test apply -f -; kubectl -n atlas-restore-test wait --for=condition=complete job/atlas-postgres-restore --timeout=900s; kubectl -n atlas-restore-test get job atlas-postgres-restore -o jsonpath='{.spec.template.spec.serviceAccountName}'`. 6. compare: `kubectl -n atlas-restore-test exec -i atlas-postgres-0 -- sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -Atf /dev/stdin' < raw/data/rowcount.sql > raw/data/atlas-restore-test-$DATE.counts.tsv; kubectl -n atlas-staging exec -i atlas-postgres-0 -- sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -Atf /dev/stdin' < raw/data/rowcount.sql \| diff - raw/data/atlas-restore-test-$DATE.counts.tsv` (N-4 form: `-i` + in-pod expansion of `POSTGRES_USER`/`POSTGRES_DB`). 7. `kubectl delete ns atlas-restore-test` | job complete; `$OBJ` non-empty; ExternalSecret Ready; restore Job `1` and `serviceAccountName` = `pg-restore`; empty diff (counts equal to live staging at dump time — re-take the staging side immediately after step 1 if staging is being written to); scratch namespace deleted | all | output |
| X-E.23 | Backup acceptance — Backup for GKE restore of kradle-staging gitea | `gcloud beta container backup-restore backups create manual-$DATE --location us-central1 --backup-plan gitea-daily --wait; gcloud beta container backup-restore restores create restore-$DATE --location us-central1 --restore-plan gitea-restore --backup=…/manual-$DATE --wait; kubectl -n kradle-restore-test run git --rm -i --restart=Never --image=alpine/git -- ls-remote http://kradle-kradle-gitea-http.kradle-restore-test.svc:3000/a5c-ai/agent-sandbox.git` (the gitea owner of the sandbox repo is the bootstrap org `a5c-ai`, `templates/gitea.yaml:112`; the repo is private — pass the restored `kradle-gitea-agent-token` inside the pod: `kubectl run` accepts only `--env` and `--overrides` (no secret-wide env flag), so use `kubectl -n kradle-restore-test run git --rm -i --restart=Never --image=alpine/git --overrides='{"spec":{"containers":[{"name":"git","image":"alpine/git","command":["sh","-c","git ls-remote http://kradle-admin:$TOKEN@kradle-kradle-gitea-http:3000/a5c-ai/agent-sandbox.git"],"env":[{"name":"TOKEN","valueFrom":{"secretKeyRef":{"name":"kradle-gitea-agent-token","key":"token"}}}]}]}}'` — the basic-auth user is `gitea.admin.username` (`kradle-admin`, `values.yaml:432`), the owner of the `kradle-agent` token minted by `gitea-token-job.yaml`; the token is consumed inside the pod, never printed) | backup `SUCCEEDED`; restore `SUCCEEDED`; refs listed (`refs/heads/main`) | all | output |
| X-E.24 | pg-backup identity is write-only | `kubectl -n atlas-staging run gsutil --rm -i --restart=Never --overrides='{"spec":{"serviceAccountName":"pg-backup"}}' --image=gcr.io/google.com/cloudsdktool/google-cloud-cli:slim -- gcloud storage ls gs://boot-464019-pg-dumps/atlas/; echo $?` (`--overrides` sets the KSA — `--serviceaccount` was removed from `kubectl run`) | permission denied (non-zero) | exit ≠ 0 | output |
| X-E.24a | pg-restore identity is read-only (mirror of X-E.24; N-1) | `kubectl -n atlas-staging run gsutil-r --rm -i --restart=Never --overrides='{"spec":{"serviceAccountName":"pg-restore"}}' --image=gcr.io/google.com/cloudsdktool/google-cloud-cli:slim -- sh -c 'gcloud storage ls gs://boot-464019-pg-dumps/atlas-staging/ >/dev/null && echo READ_OK; gcloud storage cp /dev/null gs://boot-464019-pg-dumps/atlas-staging/x-e24a-probe; echo "write-exit=$?"'` | `READ_OK`; the `cp` prints a permission-denied error and `write-exit=1`; `gcloud storage ls gs://boot-464019-pg-dumps/atlas-staging/x-e24a-probe` afterwards → not found | read succeeds, write fails, no probe object exists | output |

## Phase F — Parallel-run verification (DNS still on Azure)

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-F.1 | Wave-1 hosts on the GKE IP | `for h in gke-canary.a5c.ai kradle-staging.a5c.ai gitea.kradle-staging.a5c.ai atlas-staging.a5c.ai kradle.a5c.ai gitea.kradle.a5c.ai atlas.a5c.ai; do curl -sk --resolve $h:443:<IP> https://$h/ -o /dev/null -w "$h %{http_code}\n"; done` | `200 307 200 200 307 200 200` | exact per host | output |
| X-F.2 | Wave-2 hosts (when deployed) | same for `geekonomy.a5c.ai studio.a5c.ai` (+ `breakpoints-pro.a5c.ai` if D-9) | `200 307` | as deployed | output |
| X-F.3 | Decommissioned hosts hit the default backend | `for h in chat docs aeq bmux app app-jaeger app-staging app-jaeger-staging install-staging hub staging.web; do curl -sk --resolve $h.a5c.ai:443:<IP> https://$h.a5c.ai/ -o /dev/null -w "$h %{http_code}\n"; done` | all `404` | exact | output |
| X-F.4 | Apex/www untouched; DNS still Azure | `dig +short a5c.ai www.a5c.ai @ns41.domaincontrol.com; dig +short kradle.a5c.ai @ns41.domaincontrol.com` | `76.76.21.21` ×2; `135.234.117.214` | exact | output |
| X-F.5 | GitHub OAuth login on **all four** hosts (all client secrets regenerated once in C.0) — **manual (browser)** | hosts-file override for `kradle-staging.a5c.ai atlas-staging.a5c.ai kradle.a5c.ai atlas.a5c.ai` → `<IP>`; browser login on each; then `for ns in kradle-staging kradle; do kubectl -n $ns logs deploy/kradle-kradle-api --since=15m \| grep -ci 'invalid_client\|bad_verification'; done` | login lands in the org on all four; `0 0` | all | **manual — evidence = one screenshot per host (no tokens, no cookies) + the log output** |
| X-F.5a | The first GitHub login of the owner creates the admin `User` **in the org namespace** (N-2: `auth.js:140-150 registerLoginProfile` writes the `User` + `IdentityMapping` into `kradle-org-<KRADLE_ADMIN_ORG>` at login; `spec.admin` per `mapLoginProfileToKradleIdentity`, `auth.js:228-236`) | after X-F.5: `kubectl -n kradle-org-a5c-ai get users.kradle.a5c.ai tmuskal -o jsonpath='{.spec.admin} {.spec.organizationRef} {.metadata.labels.role}{"\n"}'; kubectl -n kradle-org-a5c-ai get identitymappings.kradle.a5c.ai github-tmuskal -o jsonpath='{.spec.user}{"\n"}'; kubectl -n kradle-org-staging-a5c-ai get users.kradle.a5c.ai tmuskal -o jsonpath='{.spec.admin}{"\n"}'; kubectl -n kradle get users.kradle.a5c.ai --no-headers 2>/dev/null \| wc -l` (the resource name is the login normalised by `normalizeName` — lowercase RFC 1123 — which for `tmuskal` is unchanged; `KRADLE_ADMIN_USERNAME` must equal it, X-D.4a (2)) | `true a5c-ai admin`; `tmuskal`; `true`; `0` (still no `User` in the release namespace — users live in org namespaces) | all | output |
| X-F.6 | Prod login via test session (additional prod auth check; the secret never touches a command line) | `printf '{"secret":"%s","username":"ci-smoke-test"}' "$(gcloud secrets versions access latest --secret kradle-prod-test-auth)" \| curl -sk --resolve kradle.a5c.ai:443:<IP> -X POST https://kradle.a5c.ai/api/auth/test-session -H 'content-type: application/json' --data @- -o /dev/null -w '%{http_code}'` (the JSON is built in a pipe and read from stdin with `--data @-`; nothing is exported to the environment or written to a file. Run this in **bash**: `printf` is a bash **builtin** there, so the value never becomes an argv of an external process — with `/usr/bin/printf` (or a shell without the builtin) it would be visible in `ps`; check with `type printf` → `printf is a shell builtin`) — the value is the single C.0 value, identical in both shells (X-C.11) | `200` | exact | output (status only) |
| X-F.7 | Webhook replay | port-forward + signed `ping` POST (05 §6) | `200`/`204`; `kubectl -n kradle-staging logs deploy/kradle-kradle-webhook-worker --since=5m \| grep -c ping` ≥ 1 | both | output |
| X-F.8 | Jitsi / prosody | `kubectl -n kradle-staging get sts kradle-prosody -o jsonpath='{.status.readyReplicas}'; kubectl -n kradle-staging exec deploy/kradle-jitsi-subchart-jicofo -- curl -s -o /dev/null -w '%{http_code}' http://kradle-prosody:5280/http-bind`; `gh workflow run g0-rt-jitsi-e2e.yml --ref staging && gh run watch` | `1`; `200`; `success` | all | run URL |
| X-F.9 | Gitea API + clone via ingress | `curl -sk --resolve gitea.kradle-staging.a5c.ai:443:<IP> https://gitea.kradle-staging.a5c.ai/api/v1/version -w '\n%{http_code}'`; in-pod `git ls-remote` using the runtime token | `200`; refs | both | output |
| X-F.10 | kradle controllers deployment healthy, error-free | `kubectl -n kradle-staging rollout status deploy/kradle-kradle-controllers; kubectl -n kradle-staging logs deploy/kradle-kradle-controllers --since=30m \| grep -ciE 'unhandled\|ECONNREFUSED\|crash'` | ok; `0` | both | output |
| X-F.11 | Repository reconcile loop (`repoReconcileIntervalMs=300000`) + org namespace controller — **the only proof that the reconcile loop works (N-2; replaces X-E.18), run in both envs** | **staging:** with a test-session cookie (X-F.6 pattern against `kradle-staging.a5c.ai` / `kradle-staging-test-auth`): `POST /api/orgs/a5c-ai/repositories` body `{"name":"xf11-probe","visibility":"internal"}` → the CR lands in **`kradle-org-staging-a5c-ai`** (`http-server.js:76` scopes the route to the org namespace): `for i in $(seq 1 120); do p=$(kubectl -n kradle-org-staging-a5c-ai get repositories.kradle.a5c.ai xf11-probe -o jsonpath='{.status.phase}' 2>/dev/null); [ "$p" = Ready ] && break; sleep 5; done; echo "$p"`; `kubectl -n kradle-staging exec deploy/kradle-kradle-gitea -- sh -c 'curl -sf -o /dev/null -w "%{http_code}" -u "$GITEA_ADMIN_USER:$GITEA_ADMIN_PASSWORD" http://localhost:3000/api/v1/repos/a5c-ai/xf11-probe'`; then `POST /api/orgs` body `{"name":"xf11-org"}` → `kubectl get ns kradle-org-staging-xf11-org -o name; kubectl -n kradle-org-staging-xf11-org get networkpolicy --no-headers \| wc -l` (the staging probe objects are left in place — X-E.16 "after" counts them). **prod:** same `POST /api/orgs/a5c-ai/repositories` against `kradle.a5c.ai` (`--resolve`, `kradle-prod-test-auth`) → `kradle-org-a5c-ai`/`xf11-probe` reaches `Ready` and the gitea repo answers 200 in `-n kradle`; then `DELETE /api/orgs/a5c-ai/repositories/xf11-probe` (`http-server.js:84`) and `kubectl -n kradle-org-a5c-ai get repositories.kradle.a5c.ai xf11-probe 2>&1 \| grep -c NotFound` — **no `Organization` is created on prod** (X-D.4b's count of one org namespace must still hold) | staging: `Ready`; `200`; `namespace/kradle-org-staging-xf11-org`; `≥ 1`. prod: `Ready`; `200`; `1` | all, within 10 min (2× `repoReconcileIntervalMs`) | output |
| X-F.12 | Dispatch chain (agent-dispatch, agent-workspace, runner, agent-session-transcript, agent-approval, agent-writeback controllers) | dispatch one run via the API → `kubectl -n kradle-org-staging-a5c-ai get jobs -o jsonpath='{.items[-1].spec.template.spec.nodeSelector}{" "}{.items[-1].spec.template.spec.volumes[?(@.name=="workspace")].emptyDir}'`; pod scheduled on a node `kubectl get node -l a5c.ai/pool=agents`; `AgentDispatchRun`/`AgentSession`/`AgentSessionTranscript` objects gain `.status`; callback 200 in api logs | `{"a5c.ai/pool":"agents"} {}`; node present; statuses set; `200` | all | output |
| X-F.13 | Remaining kradle controller modules reconcile (`agent-adapter`, `agent-gateway-config`, `agent-memory`, `agent-memory-repository-source`, `agent-persona`, `agent-project`, `agent-provider-config`, `agent-secret-config-grant`, `agent-stack`, `agent-subagent`, `agent-transport-binding`, `agent-trigger`, `artifact-registry`, `audit`, `jitsi-meeting`, `jitsi-sync`, `kradle-inference-service`, `model-route`, `notification`, `virtual-model`, `api`) | Fixture source per module (`packages/kradle/core/tests/e2e/` holds only `lifecycle.test.js`, so the unit-test fixtures are the source): take the first `kind: '<Kind>'` object literal from the named test, write it as `raw/verify/fixtures/<Kind>.yaml` with `metadata.name: xf13-<kind-lowercase>`, `metadata.namespace: kradle-org-staging-a5c-ai`, secret-bearing fields removed — `agent-adapter` → `AgentAdapter` (`tests/agent-adapter-controller.test.js`); `agent-gateway-config` → `AgentGatewayConfig` (`agent-gateway-config-controller.test.js`); `agent-memory` + `agent-memory-repository-source` → `AgentMemoryRepository`/`AgentMemorySource` (`agent-memory-controller.test.js`); `agent-persona` → `AgentDefinition` + `AgentPersona` (`agent-persona-controller.test.js`); `agent-project` → `KradleProject` (`agent-project-controller.test.js`); `agent-provider-config` → `AgentProviderConfig` (`agent-provider-config-controller.test.js`, **provider `google-vertex`**); `agent-stack` → `AgentStack` (`agent-stack-controller.test.js`); `agent-subagent` → `AgentSubagent` (`agent-subagent-controller.test.js`); `agent-transport-binding` → `AgentTransportBinding` (`agent-transport-binding-controller.test.js`); `agent-trigger` → `Pipeline` + `AgentTriggerRule` (`agent-trigger-controller.test.js`); `artifact-registry` → `ArtifactRegistry` (`artifact-registry.test.js`); `audit` → `Repository` (`audit-controller.test.js`; the audit log entry is the assertion); `jitsi-meeting` + `jitsi-sync` → `JitsiMeetingTemplate` + `JitsiMeeting` (`jitsi-meeting-controller.test.js`, `jitsi-sync-controller.test.js`); `kradle-inference-service` → `KradleInferenceService` (`kradle-inference-service.test.js`); `model-route` → `KradleModelRoute` (`model-route-controller.test.js`); `virtual-model` → `KradleVirtualModel` (`virtual-model-controller.test.js`); `notification` → `WebhookSubscription` (`notification-controller.test.js`); `api` → covered by X-F.11 (Repository create via API). For a kind whose test has no object literal, build the minimal object from the CRD schema: `kubectl explain <kind>.spec --recursive \| grep -B1 required` (CRDs installed by X-D.2d). Then for every fixture: `kubectl apply -f raw/verify/fixtures/<Kind>.yaml && for i in $(seq 1 24); do s=$(kubectl -n kradle-org-staging-a5c-ai get <kind> xf13-<kind-lowercase> -o jsonpath='{.status}'); [ -n "$s" ] && break; sleep 5; done; echo "<Kind>\t${s:+populated}" >> raw/verify/X-F.13.tsv; kubectl delete -f raw/verify/fixtures/<Kind>.yaml` | `.status` populated for every kind in the list within 120 s (one row per kind in the evidence file); `AgentProviderConfig` reaches `Ready` | 21 rows, all `populated` | `raw/verify/X-F.13.tsv` + `raw/verify/fixtures/` |
| X-F.14 | Platform controllers | `kubectl get certificate -A -o jsonpath='{range .items[*]}{.metadata.namespace}/{.metadata.name}={.status.conditions[0].status}{"\n"}{end}'` (all issued hosts `True` — pre-DNS only the canary); `kubectl get externalsecret -A -o jsonpath='{range .items[*]}{.status.conditions[0].reason}{"\n"}{end}' \| sort -u`; `kubectl -n ingress-nginx logs deploy/ingress-nginx-controller --since=1h \| grep -c 'Error'`; `kubectl get clusterpolicy -o name \| wc -l` (kyverno discovered); `kubectl -n gke-managed-… get pods` not required — `gcloud beta container backup-restore backups list --backup-plan gitea-daily --location us-central1 --format='value(state)' \| head -1` | canary `True`; `SecretSynced`; `0`; ≥ 0; `SUCCEEDED` | all | output |
| X-F.15 | No pull secrets / all requests (repeat of X-D.14 cluster-wide) | same commands | `0` / `0` | exact | output |
| X-F.16 | Vertex lane in cluster + CI | UI assistant message → `kubectl -n kradle-staging logs deploy/kradle-kradle-api --since=5m \| grep -c 'provider=google-vertex'`; `gh workflow run live-stack.yml --ref staging` → lanes `openai-gpt55`, `google-vertex` | ≥ 1; `success` | both | run URL |
| X-F.17 | Budget spend sane — **manual (console)** | Cloud Console → Billing → Reports, project `boot-464019`, current month; plus `gcloud billing budgets list --billing-account=<acct> --format='value(displayName,amount.specifiedAmount.units)'` for the threshold | < 50 % of the D-19 budget | recorded | **manual — evidence = screenshot of the report + the budget line** in `reviews/05-phase-h-cost.md` (pre-cutover line) |
| X-F.18 | Terraform drift none | `apply-gcp.yml` dispatch (plan) both stages | `No changes.` ×2 | exit 0 | run URL |
| X-F.19 | ARC dropped | `kubectl get ns arc-systems arc-runners actions-runner-system 2>&1 \| grep -c NotFound`; `gh api repos/a5c-ai/hub/actions/runners -q '.total_count'` | `3`; `0` | both | output |
| X-F.20 | Azure still untouched | `az group list --query 'length(@)'` | `33` | exact | output |
| X-F.21 | OB-2 package assembled | `ls docs/migration/azure-to-gcp/reviews/ob2-*.md` (evidence index, D-23 diff, D-24 tag, the proposed cutover day — the owner names it at OB-2; there is no D-21 window) | file | exists | file |

## Phase G — Cutover

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-G.1 | Canary record + staging then production certificate | `dig +short gke-canary.a5c.ai @ns41.domaincontrol.com`; `kubectl -n gke-canary get certificate -o jsonpath='{.items[0].status.conditions[0].status}'`; `curl -sv https://gke-canary.a5c.ai/ 2>&1 \| grep -E 'issuer:.*(STAGING)?'` before/after the tfvars flip; `curl -s https://gke-canary.a5c.ai/ -o /dev/null -w '%{http_code}'` (no `-k`) | `<IP>`; `True`; STAGING issuer then Let's Encrypt production; `200` | all | output |
| X-G.2 | Staging records + certs + public smoke | `for h in kradle-staging gitea.kradle-staging atlas-staging; do dig +short $h.a5c.ai @ns41.domaincontrol.com; done`; `kubectl -n kradle-staging get certificate kradle-tls kradle-gitea-tls -o jsonpath='{.items[*].status.conditions[0].status}'; kubectl -n atlas-staging get certificate atlas-webui-tls -o jsonpath='{.status.conditions[0].status}'`; `for h in kradle-staging.a5c.ai gitea.kradle-staging.a5c.ai atlas-staging.a5c.ai; do curl -s https://$h/ -o /dev/null -w "$h %{http_code}\n"; done`; `gh workflow run publish.yml --ref staging` → `Kradle E2E Smoke Test` | `<IP>` ×3; `True True`; `True`; `307 200 200`; `success` | all | output + run URL |
| X-G.2a | benchmarks custom domain | `dig +short benchmarks.a5c.ai @ns41.domaincontrol.com; dig +short -t TXT benchmarks.a5c.ai @ns41.domaincontrol.com`; `gcloud alpha firebase hosting … ` or `terraform output firebase_required_dns_updates` shows none pending; `curl -s https://benchmarks.a5c.ai/tb21-paper.html -o /dev/null -w '%{http_code}'` | Firebase IPs/TXT; `200` (cert may take up to 24 h) | `200` within 24 h | output |
| X-G.3 | Prod: records → certs → real login (no secret step — OAuth was regenerated once in C.0, X-C.9) | re-run X-C.9 (counts still `1` each — proves nothing was regenerated again); `kubectl -n kradle get externalsecret -o jsonpath='{range .items[*]}{.metadata.name}={.status.conditions[0].reason}{"\n"}{end}'` (all `SecretSynced`, no annotation needed); `dig +short kradle.a5c.ai gitea.kradle.a5c.ai atlas.a5c.ai @ns41.domaincontrol.com`; certificates `kradle/kradle-tls`, `kradle/kradle-gitea-tls`, `atlas/atlas-webui-tls` `True`; `curl -s https://kradle.a5c.ai/api/healthz -o /dev/null -w '%{http_code}'`; **manual:** browser GitHub login on `kradle.a5c.ai` and `atlas.a5c.ai` (screenshot, no tokens); `git clone https://gitea.kradle.a5c.ai/a5c-ai/agent-sandbox.git /tmp/x` (the bootstrap repo, owner org `a5c-ai`); one dispatch → Job in `kradle-org-a5c-ai` | `1 1 1 1`; all `SecretSynced`; `<IP>` ×3; `True` ×3; `200`; logins ok; clone ok; Job Complete | all | output + screenshots |
| X-G.3a | *(moved to Phase C as X-C.10, rev 3 — N-3: the repo secret is written once in C.0; G.3 has no secret step)* Negative check that nothing was re-set at cutover | `gh api repos/a5c-ai/babysitter/actions/secrets/KRADLE_TEST_AUTH_SECRET -q .updated_at` | the **Phase C** date (unchanged since X-C.10) | date < the G.3 date | output |
| X-G.3b | Prod e2e smoke on public host | `gh workflow run publish.yml --ref main` (or the release tag) → `Kradle E2E Smoke Test` | `success` | exact | run URL |
| X-G.4 | Wildcard flipped, propagated (Rule 2), explicit records removed, canary removed | `dig +short random-$RANDOM.a5c.ai @ns41.domaincontrol.com; dig +short random-$RANDOM.a5c.ai @ns42.domaincontrol.com; dig +short random-$RANDOM.a5c.ai @8.8.8.8` → all `<IP>`; then after deletion `dig +noall +answer kradle.a5c.ai @ns41.domaincontrol.com` shows the wildcard answer (no explicit RR) and still `<IP>`; `dig +short gke-canary.a5c.ai @ns41.domaincontrol.com` empty (after removal — or `<IP>` via wildcard, both acceptable; the explicit record must be gone from the GoDaddy UI) | as stated | all | output + GoDaddy screenshot |
| X-G.5 | All hostnames over real DNS/TLS (no `--resolve`, no `-k`) | X-F.1 + X-F.2 loops without `--resolve`/`-k`; X-F.3 loop → `404` | codes as in X-F.1/2/3 | all | output |
| X-G.6 | Apex/www unchanged | `dig +short a5c.ai www.a5c.ai @ns41.domaincontrol.com` | `76.76.21.21` ×2 | exact | output |
| X-G.7 | Azure ingress still answers (rollback path alive) | `curl -sk --resolve kradle.a5c.ai:443:135.234.117.214 https://kradle.a5c.ai/ -o /dev/null -w '%{http_code}'` | `307` | non-000 until Phase H | output |

## Phase H — Soak + decommission

| ID | Check | Exact command / action | Expected result | Pass criteria | Evidence |
|---|---|---|---|---|---|
| X-H.1 | 7-day soak log | daily X-G.5 + `gcloud storage ls gs://boot-464019-pg-dumps/atlas/ \| tail -1` (new object each day) + `gcloud beta container backup-restore backups list --backup-plan gitea-daily --location us-central1 --format='value(createTime,state)' \| tail -1` + Cloud Logging error count `gcloud logging read 'severity>=ERROR AND resource.type="k8s_container" AND resource.labels.namespace_name=~"^(kradle\|atlas)"' --freshness=24h --format='value(timestamp)' \| wc -l` | 7 daily rows; a dump per day; backups `SUCCEEDED`; error count trend recorded | 7 rows | `reviews/05-phase-h-soak.md` |
| X-H.1a | Cost checkpoint | Billing report export → daily run-rate | ≈ $10–12/day wave 1 (+ geekonomy ≈ $2/day) | recorded; deviations > 30 % explained | `reviews/05-phase-h-cost.md` |
| X-H.2 | OB-3 approval recorded | `grep -c 'OB-3: approved' docs/migration/azure-to-gcp/decisions.md` | `1` | exact | file |
| X-H.3 | Every export verification PASS before deletion | all X-E.* rows PASS in `raw/verify/` | `grep -L PASS raw/verify/X-E.*.txt` empty | empty | listing |
| X-H.4 | Ordered deletion executed (05 H.2 steps 1–11) | after each step: `az group exists -n <rg>` → `false`; final `az group list --query 'length(@)'`; `az network vnet list --query 'length(@)'`; `az ad sp list --display-name askExpertQuestion-github-actions --query 'length(@)'` (and the other two SPNs) | `false` each; `0`; `0`; `0` ×3 | all | `raw/verify/X-H.4.txt` (one line per RG with timestamp) |
| X-H.4a | tb-fleet torn down by the source-owned script | `MantisOS/babysitter-benchmarks` `scripts/destroy.sh` output per RG; `az group list --query "[?starts_with(name,'rg-tb-fleet')] \| length(@)"` | `0` | exact | output |
| X-H.4b | `rg-hub-development-westus3` deleted only after G.4 + 7 days | timestamps: X-G.4 evidence date + 7 ≤ deletion date in X-H.4 | true | date arithmetic | evidence |
| X-H.4c | `a5ctf-rg` deleted last of D1; `NetworkWatcherRG` last overall | order of lines in `X-H.4.txt` | as stated | order | file |
| X-H.4d | Residue that went with `rg-hub-development-westus3` (05 H.2 step 7 list) is gone and was never copied | `az acr show -n acrhubdevelopmentwestus3 2>&1 \| grep -c 'ResourceNotFound\|could not be found'; az aks show -g rg-hub-development-westus3 -n aks-hub-development-westus3-v2 2>&1 \| grep -c 'ResourceNotFound\|could not be found'; az network public-ip list --query "[?ipAddress=='134.33.26.182' \|\| ipAddress=='135.234.117.214'] \| length(@)"; kubectl --context gke_boot-464019_us-central1-a_a5c-boot-gke get crd -o name \| grep -c 'krate.a5c.ai'; kubectl --context gke_boot-464019_us-central1-a_a5c-boot-gke get ns -o name \| grep -cE 'vela-system\|gatekeeper-system\|app-routing-system\|arc-\|kradle-org-(default\|knack\|commander-verify)'; gcloud artifacts docker images list us-central1-docker.pkg.dev/boot-464019/a5c-images --format='value(package)' \| grep -cE 'krate-\|claude-web\|babysitter-web\|budgets\|onboard\|tokens-dispenser\|a5c-app\|a5c-install\|hub/'` | `1`; `1`; `0`; `0`; `0`; `0` | all — the residue exists neither on Azure nor on GKE/AR | output |
| X-H.5 | Archive retained | `gcloud storage ls gs://boot-464019-azure-archive/ \| grep -cE '(litellm\|tfstate\|trial-telemetry\|hub-blob\|hub-postgres\|a5c-app)/'` (plus the `<mantis*>/` prefixes of D-26/E.0.11 when exported) | ≥ `6` (exactly the six named + every mantis prefix listed in E.0.11) | ≥ 6 and each named prefix present | output |
| X-H.6 | GitHub cleanup — org | `gh api orgs/a5c-ai/actions/secrets -q '.secrets[].name' \| grep -cE '^(AZURE_APPLICATION_CLIENT_ID\|AZURE_APPLICATION_CLIENT_SECRET\|AZURE_OPENAI_API_KEY\|KUBE_CONFIG\|RUNNER_GITHUB_TOKEN\|GEMINI_API_KEY)$'; gh api orgs/a5c-ai/actions/variables --paginate -q '.variables[].name' \| grep -c '^AZURE_'` | `0`; `0` | exact | output |
| X-H.6a | GitHub cleanup — repos | `for r in babysitter company askExpertQuestion infra a5c-website; do echo $r $(gh api repos/a5c-ai/$r/actions/secrets -q '.secrets[].name' \| grep -ciE 'AZURE\|ACR_TOKEN\|KUBE_CONFIG\|ATLAS_POSTGRES_STORAGE_KEY\|ATLAS_GITHUB_CLIENT_SECRET') $(gh api repos/a5c-ai/$r/actions/variables -q '.variables[].name' \| grep -ciE 'AZURE\|ATLAS_POSTGRES_STORAGE_ACCOUNT'); done` | every line `<repo> 0 0` | all zero | output |
| X-H.6b | Azure workflows disabled in dormant repos | `for r in hub claude-web a5c-website askExpertQuestion; do gh api repos/a5c-ai/$r/actions/workflows -q '.workflows[]\|[.name,.state]\|@tsv'; done` | the listed Azure workflows `disabled_manually` | all | output |
| X-H.6c | `infra` Azure env removed | `gh api repos/a5c-ai/infra/contents/terraform/envs/azure-dev 2>&1 \| grep -c 'Not Found'` | `1` | exact | output |
| X-H.6d | `GOOGLE_CLOUD_PROJECT`/`GOOGLE_API_KEY` retired once WIF lanes are live | `gh api orgs/a5c-ai/actions/secrets -q '.secrets[].name' \| grep -cE '^(GOOGLE_CLOUD_PROJECT\|GOOGLE_API_KEY)$'`; `git grep -c 'secrets.GOOGLE_CLOUD_PROJECT\|secrets.GOOGLE_API_KEY' -- .github/workflows` | `0`; `0` | exact | output |
| X-H.7 | `krate-staging` environment deleted | `gh api repos/a5c-ai/babysitter/environments/krate-staging 2>&1 \| grep -c 'Not Found'` | `1` | exact | output |
| X-H.8 | Final state summary | `az group list --query 'length(@)'`; `gcloud container clusters list --format='value(name,status)'`; `terraform plan` both stages `No changes.` | `0`; `a5c-boot-gke RUNNING`; clean | all | `reviews/05-final-state.md` |
