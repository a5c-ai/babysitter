# Azure → GCP migration (`boot-464019`) — document index

Produced by the `azure-to-gcp-migration` babysitter run (2026-09-16). Target: Google Cloud project **`boot-464019`**. Source: Azure sponsorship subscription `794e33cd-…` + AKS `aks-hub-development-westus3-v2`. Rule of the whole effort: **source first** — every deployed thing is traced to its git origin and the origin is changed; no payload copying. No secret value appears in any document here.

| # | Document | What it is | Status |
|---|---|---|---|
| 00 | [`00-discovery-snapshot.md`](./00-discovery-snapshot.md) | Frozen pre-run snapshot (Azure, cluster, GitHub wiring, GCP state on the workstation) | input; superseded by 01–04 where they disagree |
| 01 | [`01-azure-inventory.md`](./01-azure-inventory.md) / [`.json`](./01-azure-inventory.json) | Read-only Azure + cluster inventory: 26 systems, 33 RGs, every PVC/PG/storage account, DNS, TLS, GitHub credentials, anomalies | complete |
| 02 | [`02-source-origins.md`](./02-source-origins.md) / [`.json`](./02-source-origins.json) | Git origin of every system (repo, workflow, chart, terraform, script), status (ACTIVE/STALE/ORPHAN/MANUAL), Azure coupling map C1–C18. Corrected 2026-09-16: geekonomy = `Benihakak/geekonomy`; benchmarks site, tb-fleet and `trial-telemetry` = `MantisOS/babysitter-benchmarks` | complete (corrected) |
| 03 | [`03-gcp-readiness.md`](./03-gcp-readiness.md) | GCP access verdict, owner remediation commands, reuse audit, APIs, IAM, blockers B1–B9. **§3.1 (filled 2026-09-16)** supersedes §0: `gcloud` is authenticated as `tal@a5c.ai` on `boot-464019`, the project is greenfield; remaining workstation items are ADC and the SDK/plugin update from an elevated shell. **§3.2** records the three owner decisions of 2026-09-16 | complete; §3.1/§3.2 authoritative |
| 04 | [`04-target-architecture.md`](./04-target-architecture.md) | Target design **revision 2.1**: dispositions of all 26 systems + 33 RGs, component mapping Azure → GCP with source changes, identity (WIF, SAs), secrets provenance, DNS/TLS cutover mechanics, IaC layout, cost, risks, decisions taken vs owner decisions | **approved** (review 2) with corrections N-1..N-7 applied in place; **§4.18, §2 #1 state sentence, D-16 and D-21 superseded 2026-09-16** (kradle bootstrap-only — banners in place, no revision 3); one-line notes from plan review 2 in §4.5 and §5.2 (`pg-restore@` read-only restore identity) and §4.8 (single test-auth value, repo secret written in Phase C) |
| 04-r1 | [`reviews/04-architecture-review-1.md`](./reviews/04-architecture-review-1.md) | Adversarial review of revision 1 (NOT APPROVED, F-1..F-11) | historical |
| 04-r2 | [`reviews/04-architecture-review-2.md`](./reviews/04-architecture-review-2.md) | Adversarial review of revision 2 (APPROVED; N-1..N-7 carried into 04 rev 2.1 and 05) | authoritative review |
| 05 | [`05-migration-plan.md`](./05-migration-plan.md) | **The executable plan, revision 3** (review 2's N-1..N-7 applied: read-only `pg-restore@` identity for every restore, the real chart/api bootstrap state, one `KRADLE_TEST_AUTH_SECRET` value written in Phase C, corrected commands, geekonomy worker stop as ground-rule exception (c)) — the owner's order of 2026-09-16: Phase 0 preconditions → **OB-1** → docs commit on `staging` → B GCP IaC (infra-seed fix, infra platform, infra addons) → C GitHub variables + all 13 Secret Manager values (OAuth secrets regenerated once) → A source changes (babysitter, tests first, committed locally on `staging`, verified) → **one push = D.1/D.2 staging deploy** → promote to `main` = D.3/D.4 prod deploy (kradle from the chart bootstrap, no CR import) → other repos (one task each) → E data (atlas prod restore; exports may start once B's buckets exist) → F parallel-run → **OB-2** → G cutover → H soak → **OB-3** → decommission; risk register, rollback per phase (A = `git revert`), the three owner breakpoints | ready for OB-1 (re-review pending as `reviews/05-plan-review-3.md`) |
| 05-r1 | [`reviews/05-plan-review-1.md`](./reviews/05-plan-review-1.md) | Adversarial review of plan revision 1 (NOT APPROVED, B-1..B-3, R-1..R-8) — applied in 05/06 rev 2 | historical |
| 05-r2 | [`reviews/05-plan-review-2.md`](./reviews/05-plan-review-2.md) | Adversarial review of plan revision 2 (NOT APPROVED, N-1..N-7 with file/line evidence) — applied in 05/06 rev 3 and as one-line notes in 04 §4.5/§4.8/§5.2 | historical |
| 06 | [`06-verification-checklist.md`](./06-verification-checklist.md) | Objective checks per phase (`X-<phase>.<n>`), revision 3: every hostname, stateful item, workflow job, GitHub variable and CRD controller, with exact commands, pass criteria and evidence paths; manual rows marked; new rows X-C.10/X-C.11 (test-auth), X-E.24a (`pg-restore` read-only), X-F.5a (post-login `User`); X-E.18 re-pointed at X-F.11; X-G.3a moved to X-C.10 | ready |
| — | `decisions.md` | Owner decisions (18: D-4..D-15, D-18, D-19, D-22..D-27) as answered at OB-1, the `D-16`/`D-21: superseded 2026-09-16` lines, OB-2 (incl. the cutover day) / OB-3 approvals and `no-export:` lines | created at OB-1 |
| — | `decommission-notes/<repo>.md` | For DECOMMISSION-only repos (claude-web, app, hub, askExpertQuestion by default): what runs, what is exported, what is deleted, "no code change" | created in Phase D |
| — | `reviews/05-phase-a-verification.md`, `reviews/05-phase-b-log.md`, `reviews/05-phase-h-soak.md`, `reviews/05-phase-h-cost.md`, `reviews/05-final-state.md`, `reviews/ob2-*.md` | Evidence written by the executing tasks | created as phases run |
| raw | [`raw/`](./raw/README.md) | Command outputs: `raw/azure/`, `raw/k8s/`, `raw/dns/`, `raw/github/`, `raw/gcp/` (incl. `inventory.sh`), `raw/data/` (row counts, manifests — Phase E), `raw/verify/` (one file per 06 check id) | grows during execution |

## Reading order

1. Owner deciding whether to proceed: 04 §0 (one page) → 05 header banner + §0 (the order) and §9.3 → 04 §10.B (the 18 decisions still open) → 06 (what "done" means).
2. Implementer of Phase A (babysitter source): 05 §1 only — it is self-contained (branch mechanics: commits on `staging`, one push after B and C are green; files, tests first, variable names, registry path, acceptance commands).
3. Implementer of Phase B (terraform): 04 §5, §7 → 05 §2 → 06 Phase B rows.
4. Anyone executing a cutover or a deletion: 05 §7 / §8 with 06 Phases G / H open beside it.

## Repos touched (one task each in Phase D; see 05 §4)

`a5c-ai/babysitter` (Phase A — directly on `staging`, then `main`; no feature branch), `a5c-ai/infra` (Phase B), `a5c-ai/infra-seed` (module fixes), `Benihakak/geekonomy` (rebuild, wave 2), `MantisOS/babysitter-benchmarks` (rebuild + archive pointer, wave 2), `a5c-ai/company` (content-studio, wave 2), `a5c-ai/osb` (source changes only), `a5c-ai/askExpertQuestion` (decommission by default), `a5c-ai/claude-web`, `a5c-ai/app`, `a5c-ai/hub` (decommission notes only). Not touched: `a5c-ai/a5c`, `a5c-ai/install`, `a5c-ai/mantis-proxy`, `a5c-ai/a5c-website` (GitHub cleanup only), `a5c-ai/website-a5c` / `a5c-ai/site` (Vercel, out of scope).
