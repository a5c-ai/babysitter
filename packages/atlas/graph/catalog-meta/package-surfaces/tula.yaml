nodeKind: PackageSurface
id: package:a5c-ai-genty
attributes:
  packageId: "@a5c-ai/genty"
  packageName: "@a5c-ai/genty"
  workspacePath: packages/genty
  moduleType: typescript
  surfaceKinds:
    - agent-runtime
    - bin-entrypoint
    - cli
    - llm-client
    - provider-facade
  sourceOfTruthRole: application
  publishedTo: npm
  versionPolicy: semver
  # Source: packages/genty/package.json (exports ./cli barrel + root barrel).
  # Bin: genty. Composes agent-core, agent-runtime, agent-platform, adapters,
  # and babysitter-sdk into a single installable product.
edges:
  references_path:
    - target: path:a5c-runs-dir
  exposes_subcommand:
    - target: interaction-primitive:babysitter-sdk-cli-harness-runtime
