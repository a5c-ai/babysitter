{
  "baseBranch": "staging",
  "userRequest": "$babysitter:yolo  You are an agent version tracker. Check for new releases of the ORIGINAL upstream agent CLI products — NOT our babysitter plugin packages. Build atlas, derive targets using buildTargetsFromCatalog logic from scripts/sync-external-plugin-repos.mjs, merge adapter npm install commands excluding @a5c-ai/* packages, check every upstream agent latest version via npm or authoritative web sources, compare against AgentVersion records in packages/atlas/graph/agent-stack/agent-versions/ and products/, update graph for new versions with release research and concrete capability notes, rebuild atlas, create one branch/commit/PR against staging if graph files changed, create specific agent-version-update and graph-update issues for each new agent/version pair if absent, and print a summary table for all agents checked."
}
