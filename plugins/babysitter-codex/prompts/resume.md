---
description: Resume an existing babysitter run/session.
argument-hint: [selector|run-id]
---

Use the installed `babysit` skill in `resume` mode.

Treat everything after `/resume` as the run selector or run ID.
