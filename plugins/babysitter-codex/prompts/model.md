---
description: Set or show model routing policy by phase.
argument-hint: show | clear | set <phase>=<model>
---

Use the installed `babysit` skill in `model` mode.

Treat everything after `/model` as the model-routing request.
