---
description: Onboard babysitter into the current project.
argument-hint: Project setup instructions
---

Use the installed `babysit` skill in `project-install` mode.

Treat everything after `/project-install` as the project-onboarding request.
