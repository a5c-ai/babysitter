---
description: Open observer workflow for run visibility and monitoring.
argument-hint: [watch target]
---

Use the installed `babysit` skill in `observe` mode.

Treat everything after `/observe` as the run selector or observation target.
