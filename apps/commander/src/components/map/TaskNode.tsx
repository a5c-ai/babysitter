/**
 * TaskNode — task/objective structure on the map with progress ring
 * (SPEC §3: queued → assigned → in_progress → review → done | failed).
 * Stub only — populated in the UI phase.
 */
export function TaskNode(): null {
  return null;
}
