/**
 * MapViewport — pan/zoom world layer (CSS transforms) hosting task nodes,
 * unit sprites, link layer, marquee, pings (SPEC §4/§5).
 * Stub only — populated in the UI phase.
 */
export function MapViewport(): null {
  return null;
}
