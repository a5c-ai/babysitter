/**
 * UnitSprite — agent session unit with procedural portrait, health/energy
 * bars, visual-state ring (SPEC §3). Memoized on visual-state hash (SPEC §14).
 * Stub only — populated in the UI phase.
 */
export function UnitSprite(): null {
  return null;
}
