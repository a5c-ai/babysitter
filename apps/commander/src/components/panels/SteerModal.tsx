/**
 * SteerModal — prompt input modal for the `Steer…` command, sending
 * `session.message` frames (SPEC §8). Stub only — populated in the UI phase.
 */
export function SteerModal(): null {
  return null;
}
