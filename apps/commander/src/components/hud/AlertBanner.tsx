/**
 * AlertBanner — top-center banner when approvals pending, with inline
 * Approve/Deny (SPEC §4/§5). Stub only — populated in the UI phase.
 */
export function AlertBanner(): null {
  return null;
}
