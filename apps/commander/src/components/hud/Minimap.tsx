/**
 * Minimap — fixed top-right overlay; viewport rect, pings, click-to-jump
 * (SPEC §4/§5). Canvas rendering allowed here ONLY (SPEC §11).
 * Stub only — populated in the UI phase.
 */
export function Minimap(): null {
  return null;
}
