/**
 * CommandCard — 3x4 grid of contextual commands from the microagent, with
 * Q/W/E/R/A/S/D/F/Z/X/C/V hotkey hints (SPEC §4/§5/§8).
 * Stub only — populated in the UI phase.
 */
export function CommandCard(): null {
  return null;
}
