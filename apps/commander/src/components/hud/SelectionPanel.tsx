/**
 * SelectionPanel — single: portrait + vitals; multi: unit card grid;
 * task: details + assignees (SPEC §4). Stub only — populated in the UI phase.
 */
export function SelectionPanel(): null {
  return null;
}
