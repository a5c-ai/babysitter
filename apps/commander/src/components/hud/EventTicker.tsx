/**
 * EventTicker — bottom-left clickable event stream, virtualized to last 50
 * visible (SPEC §4/§14). Stub only — populated in the UI phase.
 */
export function EventTicker(): null {
  return null;
}
