/**
 * WarRoom — full-viewport screen composing the map viewport and HUD chrome
 * (SPEC §4). Stub only — populated in the UI phase; App.tsx mounts this once real.
 */
export function WarRoom(): null {
  return null;
}
