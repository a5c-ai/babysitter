/**
 * MockBackend implementing `CommanderBackend` over the in-memory simulation
 * (SPEC §7). Swapped for the real `@a5c-ai/adapters-gateway` transport in v2.
 * Stub only — populated in the backend phase.
 */
export {};
