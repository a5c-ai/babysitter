/**
 * Mock scenario seeding (SPEC §7): 10–16 units across ≥4 adapters and
 * 6–10 tasks across 2–3 workspaces, deterministically generated from the seed.
 * Stub only — populated in the backend phase.
 */
export {};
