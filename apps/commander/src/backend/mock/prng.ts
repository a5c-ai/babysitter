/**
 * Seeded PRNG (mulberry32 or similar) for the deterministic mock simulation
 * (SPEC §7). Seed comes from `?seed=` URL param, default 42.
 * Stub only — populated in the backend phase.
 */
export {};
