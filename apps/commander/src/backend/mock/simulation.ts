/**
 * Tick-driven deterministic simulation (SPEC §7): 250ms ticks, lifecycle
 * advancement, ServerFrame emission, pausable + single-steppable.
 * Same seed + same command sequence ⇒ identical state.
 * Stub only — populated in the backend phase.
 */
export {};
