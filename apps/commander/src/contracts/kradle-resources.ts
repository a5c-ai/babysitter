/**
 * Mirrored kradle resource contracts (SPEC §2).
 * Must mirror kradle CRDs (`packages/kradle/core/docs/agents/crd-schema-spec.md`),
 * notably the `AgentDispatchRun` shape backing `CommanderTask`.
 * Populated in the contracts phase.
 */
export {};
