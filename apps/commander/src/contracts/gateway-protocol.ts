/**
 * Mirrored gateway protocol contracts (SPEC §2).
 * Must mirror `@a5c-ai/adapters-gateway` WS protocol v1
 * (`packages/adapters/gateway/src/protocol/v1.ts`) and its REST surface
 * (`gateway/src/server.ts`). Populated in the contracts phase.
 */
export {};
