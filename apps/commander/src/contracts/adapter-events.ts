/**
 * Mirrored adapter event contracts (SPEC §2).
 * Must mirror `@a5c-ai/comm-adapter` (`packages/adapters/core/src/events.ts`,
 * `session-types.ts`). Populated in the contracts phase — do not invent fields.
 */
export {};
