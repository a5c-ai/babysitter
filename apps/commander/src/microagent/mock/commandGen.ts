/**
 * Rule-based mock command generation (SPEC §8): selection state → command set,
 * mixed selections get the intersection, always ≤12 commands.
 * Stub only — populated in the microagent phase.
 */
export {};
