/**
 * Procedural icon generation (SPEC §8): hash entity id → friendly geometric
 * avatar; palette keyed by adapter (units) / taskKind badge (tasks).
 * Same id ⇒ byte-identical SVG (unit-tested).
 * Stub only — populated in the microagent phase.
 */
export {};
