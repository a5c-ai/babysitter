/**
 * Microagent interface (SPEC §8): `generateCommands(ctx)` / `generateIcon(ctx)`,
 * `CommandSpec`, `IconSpec` (`{ svg, palette }`), context summaries.
 * Stub only — populated in the microagent phase.
 */
export {};
