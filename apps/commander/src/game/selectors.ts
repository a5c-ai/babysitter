/**
 * Derived selectors over the game store (SPEC §6): resources snapshot,
 * selection summaries, fleet snapshot for the microagent.
 * Stub only — populated in the game-state phase.
 */
export {};
