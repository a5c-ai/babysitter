/**
 * Interaction grammar wiring (SPEC §5): click/shift-click/marquee selection,
 * right-click orders, hotkeys (Q..V grid, Ctrl+1..9 groups, WASD pan, Esc,
 * Space, F). Stub only — populated in the interaction phase.
 */
export {};
