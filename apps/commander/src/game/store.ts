/**
 * Zustand store (SPEC §6): single store with world/selection/camera/events/
 * alerts/meta slices. One store update per sim tick.
 * Stub only — populated in the game-state phase.
 */
export {};
